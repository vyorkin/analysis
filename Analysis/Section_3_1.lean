import Mathlib.Tactic

set_option doc.verso.suggestions false

/-!
# Analysis I, раздел 3.1: Основы теории множеств

В этом разделе мы выстраиваем версию теории множеств Цермело–Френкеля (с атомами), стараясь как
можно точнее следовать оригинальному тексту Analysis I, раздел 3.1. Вся нумерация ссылается на
оригинальный текст.

Я старался сделать перевод максимально точным перефразированием оригинального текста. Когда
приходилось выбирать между более идиоматичным Lean-решением и более точным переводом, я, как
правило, выбирал второе. В частности, местами Lean-код можно было бы "заголфить", сделав его более
элегантным и идиоматичным, но я сознательно этого избегал.

Основные конструкции и результаты этого раздела:

- Тип `Chapter3.SetTheory.Set` множеств
- Тип `Chapter3.SetTheory.Object` объектов
- Аксиома о том, что каждое множество является объектом (или может быть приведено к)
- Пустое множество {syntax term}`∅`, одноэлементные множества {syntax term}`{y}` и пары {syntax term}`{y,z}`
  (и более общие конечные кортежи) с соответствующими аксиомами
- Попарное объединение {syntax term}`X ∪ Y` с соответствующими аксиомами
- Приведение множества `A` к ассоциированному с ним типу `A.toSubtype`, являющемуся подтипом
  `Object`, и базовое API. (Это техническая конструкция, нужная для согласования теории множеств
  Цермело–Френкеля с теорией зависимых типов Lean.)
- Спецификация `A.specify P` множества `A` и предиката `P: A.toSubtype → Prop` до подмножества
  элементов `A`, удовлетворяющих `P`, и аксиома спецификации.
  TODO: как-нибудь реализовать элаборацию set-builder нотации для этого.
- Замена `A.replace hP` множества `A` через предикат
  `P: A.toSubtype → Object → Prop`, удовлетворяющий условию однозначности
  `∀ x y y', P x y ∧ P x y' → y = y'`, и аксиома замены.
- Биективное соответствие между натуральными числами Mathlib `ℕ` и множеством
  `Chapter3.Nat : Chapter3.Set` (аксиома бесконечности).
- Аксиомы регулярности, степенного множества и объединения (используются в последующих разделах
  этой главы, но не требуются здесь)
- Связь с понятием {name}`Set` из Mathlib

Остальные аксиомы теории множеств Цермело–Френкеля обсуждаются в следующих разделах.

Некоторые технические замечания:
- У Mathlib, разумеется, есть собственное понятие {name}`Set` (точнее, тип `Set X`, связанный с
  каждым типом `X`), несовместимое с определяемым здесь понятием `Chapter3.Set`, хотя мы и
  постараемся максимально согласовать нотацию. Это порождает некоторый конфликт обозначений:
  например, иногда придётся явно указывать `(∅:Chapter3.Set)` вместо просто {syntax term}`∅`,
  чтобы обозначить, что имеется в виду версия пустого множества из `Chapter3.Set`, а не из Mathlib,
  и аналогично для прочих нотаций, определённых здесь.
- В Analysis I мы решили работать с "нечистой" теорией множеств, в которой `Object`-ов может быть
  больше, чем просто `Set`-ов. В теории типов Lean это требует, чтобы `Chapter3.Set` и
  `Chapter3.Object` были различными типами. Иногда из-за этого приходится использовать приведение
  `(X: Chapter3.Object)` множества `Chapter3.Set` `X` к `Chapter3.Object`: обычно это нужно при
  работе с множествами множеств.
- Строго говоря, множество `X:Set` не является типом; тем не менее мы будем приводить множества к
  типам, а именно к подтипу `Object`. Аналогичное приведение используется и в формализации множеств
  в Mathlib.
- После завершения этой главы понятие `Chapter3.SetTheory.Set` будет вытеснено стандартным понятием
  {name}`Set` из Mathlib (точнее, типом `Set X` множества в заданном типе `X`). Однако из-за
  различных технических несовместимостей между теорией множеств и теорией типов мы не будем
  пытаться построить полную эквивалентность между этими двумя понятиями множества. (Из-за этого
  вся эта глава оказывается необязательной с точки зрения остальной части книги, но мы сохраняем
  её ради педагогической ценности.)

## В чём я путаюсь

- Экстенсиональность — про равенство множеств: A = B ↔ ∀ x, x ∈ A ↔ x ∈ B.
- Тождество неразличимых (Лейбниц) — про равенство объектов: x = y ↔ ∀ P, P(x) ↔ P(y).
- Однозначность/функциональность hP — про то, что отношение P ведёт себя как функция:
                                      P(x,y) ∧ P(x,y') → y = y'.

Все три утверждения имеют одну и ту же логическую форму:
  ∀ a b, (a и b одинаково "проходят" один и тот же тест)
Но роли у них разные, и именно поэтому смешивать названия было ошибкой.

Это, по сути, теоретико-множественное/логическое проявление того,
что в теории категорий называется леммой Йонеды — идея, что
"объект есть не более чем совокупность способов его пронаблюдать/сравнить с другими".

## Советы от прошлых пользователей

Пользователи, прошедшие упражнения этого раздела, могут присылать свои советы для будущих
пользователей в виде PR.

- (Добавьте совет сюда)
-/

namespace Chapter3

/- The ability to work in multiple universe is not relevant immediately, but
becomes relevant when constructing models of set theory in the Chapter 3 epilogue. -/
universe u v

/-- Аксиомы теории множеств Цермело–Френкеля с атомами. -/
class SetTheory where
  Set : Type u -- Axiom 3.1
  Object : Type v -- Axiom 3.1
  -- `↪` — тип `Function.Embedding`: пара (функция + доказательство инъективности).
  --
  -- structure Embedding (α : Sort*) (β : Sort*) where
  --   toFun : α → β
  --   inj' : Injective toFun
  --
  -- Метод `.inj'` извлекает инъективность: `∀ a b, f a = f b → a = b`.
  --
  set_to_object : Set ↪ Object -- Axiom 3.1
  -- Утверждение о том, что элемент входит в множество (принадлежит множеству).
  mem : Object → Set → Prop -- Axiom 3.1
  -- Два множества равны, если у них одинаковые элементы (экстенсиональность).
  extensionality X Y : (∀ x, mem x X ↔ mem x Y) → X = Y -- Axiom 3.2
  -- Существует множество без элементов.
  emptyset : Set -- Axiom 3.3
  -- У пустого множества действительно нет элементов.
  emptyset_mem x : ¬ mem x emptyset -- Axiom 3.3
  -- Множество, состоящее ровно из одного элемента y.
  singleton : Object → Set -- Axiom 3.4
  -- x ∈ {y} тогда и только тогда, когда x = y.
  singleton_axiom x y : mem x (singleton y) ↔ x = y -- Axiom 3.4
  -- Объединение двух множеств.
  union_pair : Set → Set → Set -- Axiom 3.5
  -- x ∈ X ∪ Y тогда и только тогда, когда x ∈ X или x ∈ Y.
  union_pair_axiom X Y x : mem x (union_pair X Y) ↔ (mem x X ∨ mem x Y) -- Axiom 3.5

  -- Ниже в аксиомах 3.6/3.7 встречается запись `Subtype (mem . A)`.
  -- Точка здесь — это плейсхолдер-синтаксис Lean (аналог `·`) для анонимной функции:
  -- Запись `mem . A` разворачивается в `fun x => mem x A`.
  -- То есть в предикат «x принадлежит A».
  -- Тогда `Subtype (fun x => mem x A)` — это подтип `Object`, состоящий
  -- из элементов, принадлежащих A – пары `⟨x, доказательство mem x A⟩`.
  -- Именно по такому подтипу и должен "пробегать" предикат `P` в `specify`/`replace`.

  -- TL;DR интуиция:
  --
  -- Спецификация - filter
  -- Замена       - map

  -- Спецификация/выделение:
  -- `specify A P` — подмножество A из тех x, для которых P x истинно.
  -- По сути вот что это такое: `{x ∈ A | P(x)}`, и результат всегда есть подможество A.
  specify A (P : Subtype (mem . A) → Prop) : Set -- Axiom 3.6
  -- Аксиома спецификации.
  specification_axiom A (P : Subtype (mem . A) → Prop) :
    (∀ x, mem x (specify A P) → mem x A) ∧ (∀ x, mem x.val (specify A P) ↔ P x) -- Axiom 3.6
  -- Вот как можно сформулировать это ^^^ на естественном языке:
  -- 1) Если `x` входит в выделенное подмножество `specify A P`, то `x` входит и в исходное множество `A`.
  -- 2) Вхождение `x` в выделенное подмножество `specify A P` равносильно истинности предиката `P` для этого `x`.
  --
  -- Итак, у нас есть аксиома спецификации/выделения
  -- (самое выделение `specify` и аксиома `specification_axiom`, Axiom 3.6):
  -- она позволяет взять множество `A` и предикат `P : Subtype (mem . A) → Prop`, и
  -- получить подмножество `A`, состоящее из тех `x`, для которых `P x` истинно.
  -- Результат всегда лежит внутри уже существующего множества `A`.
  -- Это делает `specify` безопасной (не даёт парадоксов вроде парадокса Рассела),
  -- но в то же время это условие делает её и слабой —
  -- невозможно с её помощью получить множество, чьи элементы "не помещаются" в `A`.

  -- Аксиома замены.
  --
  -- В математике постоянно нужно применять функцию к элементам множества и собирать результаты.
  -- Например, `{x² : x ∈ A}`, `{{x} : x ∈ A}`, `{f(x) : x ∈ A}` для произвольной функции `f`.
  -- Результаты этих операций — это, вообще говоря, не элементы `A` и
  -- не элементы никакого заранее известного множества.
  -- Из `specify` (выделения подмножества) такое множество результатов получить нельзя —
  -- там неоткуда взять "объемлющее" множество, из которого мы бы выделяли подмножество.
  --
  -- Эту "дыру" и закрывает аксиома замены (replacement).
  --
  -- Она ПОСТУЛИРУЕТ:
  -- Несмотря на то, что образы `y` могут быть совершенно произвольными объектами
  -- (не подмножеством `A`, не подмножеством чего-либо заранее известного),
  -- их совокупность `{y : ∃ x ∈ A, P(x,y)}` гарантированно является множеством.
  -- То есть, в отличие от `specify`, результат не обязан быть подмножеством `A`.
  --
  -- Обрати внимание, что под образом здесь понимается нечто не совсем привычное,
  -- а именно 2 факта о каждом `y`:
  --
  -- 1) `∃ x ∈ A` - существование элемента прообраза (A)
  -- 2) `P(x, y)` - и связь `x` с `y`
  --
  -- Условие `hP` про однозначность/функциональность
  -- (если `P x y` и `P x y'`, то `y = y'`)
  -- превращает это отношение в функцию
  -- (в математическом смысле "каждому x — не более одного y"),
  -- хотя как объект теории множеств такая функция ещё не существует.
  --
  -- Итак, Axiom 3.7:
  replace A (P : Subtype (mem . A) → Object → Prop)
    (hP : ∀ x y y', P x y ∧ P x y' → y = y') : Set
  --
  -- `P x y` читается как "x связан с y" (или "x переходит в y").
  -- Первый аргумент `x` обязан быть элементом `A` (тип `Subtype (mem . A)`).
  -- А вот второй аргумент `y` — это уже произвольный объект из всего типа `Object`,
  -- любой объект вселенной, не привязанный ни к `A`, ни к какому-то другому известному множеству.
  --
  -- Берём однозначную (функциональную, за это отвечает `hP`) связь, определённую на `A`, и
  -- её "образы" `y` могут оказаться любыми объектами вселенной — не обязательно элементами `A`.
  -- Несмотря на это, совокупность всех таких образов `{y : ∃ x ∈ A, P(x,y)}`
  -- всё равно образует корректное множество `Set`.
  --
  -- Пример: пусть `A` — множество чисел, а `P x y` означает
  -- "y — это одноэлементное множество {x}, приведённое к объекту".
  -- Тогда образы `y` — это множества (через `set_to_object`), а вовсе не элементы `A`.
  -- Именно поэтому второй аргумент предиката `P` типизирован как `Object`, а не как `Subtype (mem . A)`.
  --
  -- Axiom 3.7:
  replacement_axiom A (P : Subtype (mem . A) → Object → Prop)
    (hP : ∀ x y y', P x y ∧ P x y' → y = y') : ∀ y, mem y (replace A P hP) ↔ ∃ x, P x y

  -- Вот в чём разница по существу с specify:
  --
  -- specify — "сузить"/"отфильтровать" существующее множество до подмножества (безопасная, слабая операция).
  -- replace — "спроецировать"/"отобразить" элементы множества через функциональную связь наружу,
  --           в произвольные объекты, и получить новое множество образов (гораздо более сильная операция).

  -- Именно из-за этой силы аксиома замены (`replacement_axiom`)
  -- исторически и была добавлена Френкелем к системе Цермело:
  -- без неё нельзя было доказать существование даже довольно простых множеств.
  -- Например: {{0}, {1}, {2}, …} — множество всех одноэлементных множеств натуральных чисел
  -- (ну ты понял, да? ведь множества не являются натуральными числами).
  --
  -- Аксиома 3.4 (`singleton`) даёт `{n}` только по одному, для одного
  -- конкретного `n` за раз, а `specify` (Axiom 3.6) не годится,
  -- так как `{n}` — это не элемент `ℕ` и взять его подмножеством просто неоткуда.
  --
  -- А вот `replace` с `A = ℕ` и `P(n, y) := y = {n}` как раз и даёт такое множество:
  -- `hP` выполнено тривиально (у каждого `n` ровно один образ `{n}`).
  --
  -- При этом парадоксов `replacement_axiom` не порождает именно благодаря
  -- условию `hP` (однозначность/функциональность) и тому,
  -- что она стартует от уже существующего множества `A`, а не от любого "свойства вообще"
  -- (как в наивной, парадоксальной схеме понимания).

  -- Постулирует существование некоторого множества с именем `nat`.
  -- Само по себе это ничего не гарантирует —
  -- иметь тип Object/Set недостаточно, чтобы сказать, что это "именно натуральные числа".
  nat : Set -- Axiom 3.8
  -- А вот это и делает nat "натуральными числами".
  nat_equiv : ℕ ≃ Subtype (mem . nat) -- Axiom 3.8
  --
  -- Здесь:
  -- 1) `Subtype (mem . nat)` – подтип `Object`,
  --                            состоящий из элементов, принадлежащих множеству `nat`.
  -- 2) `≃` — это `Equiv` из Mathlib:
  --          1) Пара функций туда/обратно (`toFun`, `invFun`)
  --          2) Доказательства, что они взаимно обратны (`left_inv`, `right_inv`)
  --    Короче говоря, это конкретное, явное "отождествление" одного с другим.
  --
  -- Откуда берутся сами `toFun`/`invFun` и
  -- доказательства их взаимной обратности, если мы нигде их не строим?
  -- Фишка в том, что здесь мы лишь постулируем существование
  -- терма такого типа, а не предъявляем его.
  --
  -- Поскольку сам `Equiv` (`≃`) это структура с четырьмя полями
  -- (`toFun`, `invFun`, `left_inv`, `right_inv`),
  -- то их обязан предоставить любой, кто предъявит
  -- конкретный экземпляр `SetTheory` (модель этих аксиом).
  -- В этом файле такой экземпляр никогда не строится —
  -- мы работаем аксиоматически через `variable [SetTheory]`,
  -- так что `nat_equiv` остаётся предполагаемым термом,
  -- а `.toFun`/`.invFun` ниже — это просто проекции полей `Equiv`.

  -- Теперь у нас есть явная функция `nat_equiv.toFun : ℕ → Subtype (mem . nat)`,
  -- которая берёт обычное натуральное число `n : ℕ` и
  -- возвращает соответствующий ему элемент множества `nat`,
  -- и обратная функция `nat_equiv.invFun`, которая берёт элемент `nat` и возвращает,
  -- каким числом `ℕ` он является — и они гарантированно взаимно обратны.

  -- Вот простейший пример того, что позволяет делать структура `Equiv` и каким образом:
  --
  -- Здесь `toFun` превращает `Bool` в `Fin 2`, а `invFun` — обратно,
  -- Функции `left_inv`/`right_inv` доказывают, что это действительно взаимно обратные операции
  -- (проверяется перебором всех вариантов, `Bool` и `Fin 2` конечны).
  --
  -- def boolFinEquiv : Bool ≃ Fin 2 where
  --   toFun x := if x then 1 else 0
  --   invFun n := n = 1
  --   left_inv := by decide
  --   right_inv := by decide

  -- Axiom 3.10 (регулярность/фундирование, foundation/regularity axiom)
  --
  -- Запрещает бесконечные нисходящие цепочки принадлежности типа таких:
  -- `(… ∈ x₂ ∈ x₁ ∈ A)`
  -- В частности, множество не может содержать само себя.
  --
  -- Примеры того, что именно запрещено (такие множества не могут существовать):
  -- 1) Множество, содержащее само себя: A = {A}. Тогда A ∈ A ∈ A ∈ … — бесконечная
  --    нисходящая цепочка из одного и того же элемента, повторяющегося без конца.
  -- 2) Цикл из двух множеств: A = {B}, B = {A}. Тогда A ∈ B ∈ A ∈ B ∈ … —
  --    снова бесконечный спуск, только чередующий A и B, вместо одного A.
  --
  -- Разберём что написано в формальной записи аксиомы регулярности ниже:
  --
  -- 1. Условие `hA : ∃ x, mem x A` — значит, что "A непусто".
  -- 2. У `A` есть элемент `x` и если этот `x` сам является множеством `S` (`x` = `S`),
  --    то `S` и `A` не имеют общих элементов — ни один элемент `A` не лежит внутри `S`.
  --
  regularity_axiom A (hA : ∃ x, mem x A) :
    ∃ x, mem x A ∧ ∀ S, x = set_to_object S →
      ¬ ∃ y, mem y A ∧ mem y S -- Axiom 3.10
  --
  -- Условие `∀ S, x = set_to_object S → …` учитывает, что в этой "нечистой" теории
  -- множеств не каждый `Object` обязан быть множеством — есть ещё "атомы".
  -- Если `x` — атом, то никакого такого `S` не существует, и импликация
  -- выполняется тривиально (истинна на пустом основании).

  -- Axiom 3.11 (power set axiom)
  --
  -- В книге Tao она называется "power set axiom", но по сути это
  -- аксиома существования множества функций между двумя множествами.
  --
  -- `pow X Y` — это множество, обозначаемое в книге `X^Y`,
  -- состоящее из ВСЕХ функций из `Y` в `X`.
  pow : Set → Set → Set
  --
  -- То есть `pow X Y` — это множественный аналог типа функций
  -- `Subtype (mem . Y) → Subtype (mem . X)`.
  --
  -- Тут возникает проблема:
  -- `mem` требует, чтобы элемент `Set` имел тип `Object`,
  -- но функция `f : Y → X` имеет тип "функция",
  -- поэтому `mem f (...)` даже не типизируется.
  --
  -- Значит, прежде чем сказать `F ∈ pow X Y`,
  -- функцию сначала нужно закодировать как `Object` (как ID в базе данных) —
  -- это делает `function_to_object` ниже.

  -- Инъективное вложение функций `Y → X` в `Object`
  -- (`↪`, как и `set_to_object` для множеств).
  function_to_object (X : Set) (Y : Set) :
    (Subtype (mem . X) → Subtype (mem . Y)) ↪ Object -- Axiom 3.11

  -- Axiom 3.11 (`powerset_axiom`) связывает эти два понятия выше:
  --
  -- Объект `F` лежит в `pow X Y` тогда и только тогда,
  -- когда `F` — это "код"/ID (через `function_to_object`) какой-то функции `f : Y → X`.
  --
  powerset_axiom (X : Set) (Y : Set) (F : Object) :
    mem F (pow X Y) ↔ ∃ f : (Subtype (mem . Y) → Subtype (mem . X)), function_to_object Y X f = F
  --
  -- Почему это называется "power set":
  --
  -- Если взять в качестве `X` двухэлементное множество `{true, false}`,
  -- то функции `Y → {true, false}` —
  -- это ровно индикаторные (характеристические) функции подмножеств `Y`
  -- (такая функция отвечает на вопрос входит ли какой-то элемент в множество `Y` или нет).
  -- Эти индикаторные функции находятся во взаимно однозначном соответствии с подмножествами `Y`.
  -- Значит `pow {true,false} Y` "изоморфно" обычному степенному множеству `P(Y)`.
  -- Общая аксиома про множество функций — это обобщение обычной аксиомы степенного множества.
  --
  -- Степенное множество `P(Y)` — это множество всех подмножеств `Y`.
  -- Пример: для `Y = {1, 2}` имеем `P(Y) = {∅, {1}, {2}, {1,2}}`.

  -- Axiom 3.12 — "большое" объединение (union of a set of sets),
  -- в отличие от `union_pair` (Axiom 3.5), которая объединяет ровно два множества за раз.
  --
  -- Здесь `A` — это множество, элементы которого сами являются множествами
  -- (закодированными через `set_to_object`).
  --
  -- `union A` — это объединение ВСЕХ этих множеств:
  --
  -- `x ∈ union A` тогда и только тогда, когда найдётся `S`, такое
  -- что `S ∈ A` (как объект) и `x ∈ S`. То есть `union A = ⋃_{S ∈ A} S`.
  --
  -- Пример: если `A = {X, Y}` (как объекты), то `union A = X ∪ Y` — обычное `union_pair`
  -- получается как частный случай. Но в отличие от `union_pair`, `A` может содержать сколько
  -- угодно (в том числе бесконечно много) множеств — и `union` объединит их все за один раз.
  union : Set → Set -- Axiom 3.12
  union_axiom A x : mem x (union A) ↔ ∃ S, mem x S ∧ mem (set_to_object S) A -- Axiom 3.12

-- Это позволяет использовать `Set` и `Object` вместо `SetTheory.Set` и `SetTheory.Object`.
export SetTheory (Set Object)

-- "Атомы" — это объекты (Object), которые не являются множествами:
-- у них нет элементов, но пустым множеством они тоже не являются.
-- В стандартной ZF всё суть множества; здесь Object шире Set,
-- а атомы это, например, числа, точки и т.п., т.е. примитивные объекты.

-- Делает экземпляр класса типов SetTheory неявным параметром всех последующих
-- определений и теорем — то есть любая теорема ниже автоматически предполагает, что аксиомы
-- теории Цермело–Френкеля с "атомами" выполнены, без явного указания этого в каждом объявлении.
--
-- `variable` не создаёт объявления сама по себе, а лишь запоминает переменную
-- (здесь — безымянный аргумент типа `SetTheory`) и автоматически добавляет её в
-- сигнатуру КАЖДОГО следующего def/theorem, который на неё ссылается
-- (то есть упоминает `Set`, `Object`, `mem` и т.п. — а это как раз поля/производные `SetTheory`).
-- Объявления, которые её не используют, эту переменную вообще не получают.
--
-- Квадратные скобки `[...]` (а не круглые `(...)` или фигурные `{...}`) означают, что
-- это instance-implicit аргумент — Lean ищет подходящее значение
-- автоматически через typeclass resolution (`inferInstance`).
--
-- Например, следующее объявление ниже:
--   instance SetTheory.objects_mem_sets : Membership Object Set where ...
-- на самом деле elaborates (разворачивается компилятором) примерно в:
--   instance SetTheory.objects_mem_sets [inst : SetTheory] :
--     Membership SetTheory.Object SetTheory.Set where ...
-- Без `variable [SetTheory]` пришлось бы писать `[SetTheory]` вручную в
-- каждом отдельном def/theorem, где используются Set/Object/mem.
--
-- Важно: конкретного экземпляра `SetTheory` нигде в файле не предъявляется —
-- то есть мы пока не доказываем, что такая теория вообще непротиворечива (существует модель),
-- а лишь формулируем и доказываем теоремы, которые верны для ЛЮБОГО экземпляра,
-- удовлетворяющего этим аксиомам.
variable [SetTheory]

-- `Membership` — это стандартный тайп-класс Lean/Mathlib, который стоит за нотацией `a ∈ s`:
--   class Membership (α : outParam (Type u)) (γ : Type v) where
--     mem : γ → α → Prop
-- `γ` — тип "контейнера" (у нас — `Set`), `α` — тип элементов (у нас — `Object`), а поле
-- `mem : γ → α → Prop` — собственно отношение принадлежности (контейнер, затем элемент).
-- Именно этот класс использует любой контейнер в Lean/Mathlib для нотации `∈`
-- (`List`, `Finset`, мatлибовский `Set` и т.д.) — не только наш `Set` из этой теории.
--
-- Зачем это здесь: у нас уже ЕСТЬ примитивное отношение `mem : Object → Set → Prop`
-- (Axiom 3.1), но это обычная функция — писать `mem x X` не так удобно и не совпадает
-- с общепринятой нотацией `x ∈ X`, которую использует и сам учебник, и весь Mathlib.
-- Дав экземпляр `Membership Object Set`, мы позволяем Lean понимать `x ∈ X` как ссылку на наш `mem`.
--
-- Замечание про `outParam`: поскольку `α` помечен как `outParam`, Lean определяет тип
-- элементов ПО типу контейнера (здесь — по `Set`), а не наоборот. Поэтому достаточно
-- написать `X : Set`, и Lean сам выведет, что `x` в `x ∈ X` обязан иметь тип `Object`
-- — явно указывать `Object` не требуется.

/-- Definition 3.1.1 (объекты могут быть элементами множеств) -/
instance SetTheory.objects_mem_sets : Membership Object Set where
  mem X x := mem x X

-- Теперь можно использовать нотацию `∈` между нашими `Object` и `Set`.
example (X : Set) (x : Object) : x ∈ X ↔ SetTheory.mem x X := by rfl

-- `Coe` — это тайп-класс Lean, отвечающий за НЕЯВНЫЕ приведения типов:
--   class Coe (α : Sort u) (β : Sort v) where
--     coe : α → β
-- Когда Lean ожидает терм типа `β`, а вы даёте терм типа `α`, для которого есть
-- инстанс `Coe α β`, компилятор сам вставляет вызов `coe`, вставляя `↑x` вместо `x`.
--
-- Зачем он нужен здесь: `Set` и `Object` — разные Lean-типы (см. заметку про "нечистую"
-- теорию множеств выше), хотя математически каждое множество ЯВЛЯЕТСЯ объектом (Axiom 3.1).
-- У нас уже есть примитивная инъекция `set_to_object : Set ↪ Object`, но это обычная функция —
-- пришлось бы всюду писать `set_to_object X` вручную. Инстанс `Coe Set Object` позволяет
-- Lean вставлять это приведение автоматически везде, где ожидается `Object`, а передан `Set`
-- (например, `(X : Object)`, как показано в примере ниже).

/-- Axiom 3.1 (множества являются объектами, приведение типа). -/
instance SetTheory.sets_are_objects : Coe Set Object where
  coe X := set_to_object X

-- Теперь при необходимости можно рассматривать `Set` как `Object`.
example (X : Set) : (X : Object) = SetTheory.set_to_object X := rfl

/-- Axiom 3.1 (множества являются объектами, инъективность). -/
theorem SetTheory.Set.coe_eq {X Y : Set} (h : (X : Object) = (Y : Object)) : X = Y :=
  -- `set_to_object : Set ↪ Object` — инъективное вложение (`Function.Embedding`), то есть
  -- функция `set_to_object` вместе с доказательством своей инъективности внутри.
  -- `.inj'` достаёт из этого доказательство инъективности:
  -- для функции `set_to_object` оно имеет вид `set_to_object a = set_to_object b → a = b`.
  --
  -- А `(X : Object)` — это как раз `set_to_object X` (через инстанс `Coe Set Object` выше),
  -- поэтому наша гипотеза `h : (X : Object) = (Y : Object)` — это в точности
  -- `set_to_object X = set_to_object Y`. Подставляя её в `.inj'`
  -- (с `a := X`, `b := Y`), получаем ровно то, что нужно: `X = Y`.
  set_to_object.inj' h

/-- Axiom 3.1 (множества являются объектами, лемма-эквивалентность). -/
-- Нужно доказать `(X : Object) = (Y : Object) ↔ X = Y` — то есть сразу обе импликации.
-- То есть, если они равны как объекты, то они равны как множества и на оборот.
--
-- `Iff` — это структура из двух полей: доказательства "→" и доказательства "←".
-- Анонимный конструктор `⟨_, _⟩` как раз и собирает `↔` из такой пары: `⟨доказательство →, доказательство ←⟩`.
--
-- Первый компонент, `coe_eq` — это готовое доказательство направления "→"
-- (`(X : Object) = (Y : Object) → X = Y`), уже доказанное отдельной теоремой чуть выше —
-- мы просто подставляем его как есть.
--
-- Второй компонент, `by rintro rfl; rfl` — доказательство направления "←"
-- (`X = Y → (X : Object) = (Y : Object)`). `rintro rfl` вводит гипотезу `X = Y` и сразу же
-- ею пользуется, заменяя `Y` на `X` всюду в цели (это короткая запись для `intro h; subst h`).
-- После такой замены цель `(X : Object) = (Y : Object)` превращается в
-- `(X : Object) = (X : Object)`, которая замыкается тривиальным `rfl`.
--
-- @[simp] позволяет тактите `simp` автоматически
-- убирать приведение типов в равенствах множеств.
@[simp]
theorem SetTheory.Set.coe_eq_iff (X Y : Set) : (X : Object) = (Y : Object) ↔ X = Y :=
  ⟨coe_eq, by rintro rfl; rfl⟩

/-- Axiom 3.2 (равенство множеств).
Атрибут {attr}`@[ext]` позволяет тактике {tactic}`ext` работать с множествами.
Здесь мы просто ссылаемся на нашу уже существующую aксиому экстенсиональности. -/
@[ext]
theorem SetTheory.Set.ext {X Y : Set} (h : ∀ x, x ∈ X ↔ x ∈ Y) : X = Y :=
  extensionality X Y h

-- Теперь нам доступно Set.ext_iff:

-- Axiom 3.2 (равенство множеств, ext_iff)
#check SetTheory.Set.ext_iff -- {X Y : Set} : X = Y ↔ ∀ (x : Object), x ∈ X ↔ x ∈ Y

-- `EmptyCollection` — это тайп-класс Lean/Mathlib, который стоит за нотацией `∅`:
--   class EmptyCollection (α : Type u) where
--     emptyCollection : α
-- Он даёт просто одно значение типа `α` — «пустой» элемент этого типа
-- (для `List` это `[]`, для `Finset` — пустой конечный набор, и т.д.).
--
-- У нас уже есть примитивная константа `emptyset : Set` (Axiom 3.3),
-- но писать `emptyset` вместо привычной `∅` не так удобно.
-- Дав экземпляр `EmptyCollection Set`, мы позволяем
-- Lean понимать нотацию `∅` (в контексте `Set`) как ссылку на наш `emptyset`.
instance SetTheory.Set.instEmpty : EmptyCollection Set where
  emptyCollection := emptyset

-- Теперь можно использовать нотацию `∅` для ссылки на `SetTheory.emptyset`.
example : ∅ = SetTheory.emptyset := rfl

-- Делаем всё, что определено в `SetTheory.Set.*`, доступным напрямую.
open SetTheory.Set

/--
  Axiom 3.3 (пустое множество).
  Здесь мы просто ссылаемся на нашу определенную выше аксиому.
  Замечание: в некоторых случаях может понадобиться явно привести {lean}`(∅ : Set)` к {name}`Set`
  из-за уже существующей в Mathlib нотации теории множеств.
-/
@[simp]
theorem SetTheory.Set.not_mem_empty : ∀ x, x ∉ (∅ : Set) :=
  emptyset_mem

/-- У пустого множества нет элементов. -/
theorem SetTheory.Set.eq_empty_iff_forall_notMem {X : Set} : X = ∅ ↔ (∀ x, x ∉ X) := by
  constructor
  · intro h
    rw [h]
    exact not_mem_empty -- Axiom 3.3 : ∀ x, x ∉ (∅ : Set)
  · intro h
    -- ext x
    -- ^^^ Использование тактики ext эквивалентно следующему:
    rw [Set.ext_iff] -- Axiom 3.2 : (h : ∀ x, x ∈ X ↔ x ∈ Y) : X = Y
    intro x
    constructor
    · intro hx
      specialize h x
      contradiction
    · intro he
      have hc := not_mem_empty x -- ∀ x, x ∉ (∅ : Set)
      contradiction

/-- Более короткий вариант: после `Set.ext_iff` цель сводится к `∀ x, x ∈ X ↔ x ∈ ∅`, а
    `x ∈ ∅` уже упрощается тактикой `simp`, так как `not_mem_empty` помечена `@[simp]`. -/
theorem SetTheory.Set.eq_empty_iff_forall_notMem' {X : Set} : X = ∅ ↔ (∀ x, x ∉ X) := by
  rw [Set.ext_iff]
  simp

/-- То же самое, но без `simp`: применяем `not_mem_empty` и `(h x).mp` напрямую тактикой
    `apply`, вместо того чтобы сводить `x ∈ ∅` к `False` автоматически. -/
theorem SetTheory.Set.eq_empty_iff_forall_notMem'' {X : Set} : X = ∅ ↔ (∀ x, x ∉ X) := by
  rw [Set.ext_iff]
  constructor
  · intro h x hx
    apply not_mem_empty x
    apply (h x).mp
    exact hx
  · intro h x
    constructor
    · intro hx
      have hh := h x
      exact absurd hx hh
    · intro hx
      have hne := not_mem_empty x
      exact absurd hx hne

/-- Пустое множество единственно. -/
theorem SetTheory.Set.empty_unique : ∃! (X : Set), ∀ x, x ∉ X := by
  -- `existsUnique_of_exists_of_unique` : (∃ x, p x) → (∀ y₁ y₂, p y₁ → p y₂ → y₁ = y₂) → ∃! x, p x
  -- Она сводит доказательство `∃! x, p x` к двум отдельным подцелям:
  -- 1) существованию элемента с свойством p
  -- 2) его единственности (совпадению любых двух таких элементов).
  --
  -- `apply` расщепляет цель `∃! X, ∀ x, x ∉ X` на эти две подцели через `·`-блоки ниже.
  --
  -- Короче: существование уникального = существование ∧ уникальность
  -- Существование: ∃ x, p x
  -- Уникальность: ∀ (y₁ y₂ : α), p y₁ → p y₂ → y₁ = y₂
  apply existsUnique_of_exists_of_unique
  · use ∅
    exact not_mem_empty -- Axiom 3.3: ∀ x, x ∉ (∅ : Set)
  · -- Отсюда сразу видно, что это экстенсиональность.
    intro X Y hx hy
    ext x -- (∀ x, x ∈ X ↔ x ∈ Y) ↔ X = Y
    -- rw [Set.ext_iff]; intro x
    specialize hx x
    specialize hy x
    constructor
    · intro hx'; contradiction
    · intro hy'; contradiction

/-- Lemma 3.1.5 (единственный выбор) -/
-- Если есть непустое мн-во, то можно выбрать хотя бы один его элемент,
-- т.е. оно состоит хотя бы из одного элемента.
lemma SetTheory.Set.nonempty_def {X : Set} (h : X ≠ ∅) : ∃ x, x ∈ X := by
  -- Это доказательство написано так, чтобы следовать структуре оригинального текста.
  -- Докажем от противного, предположим обратное.
  by_contra! h
  have claim (x : Object) : x ∈ X ↔ x ∈ (∅ : Set) := by
    simp [h, not_mem_empty]
  apply ext at claim
  contradiction

-- Вот это самое понятное доказательство, основанное на
-- введении промежуточной гипотезы, которая доказывается при помощи двух противоречий.
lemma SetTheory.Set.nonempty_def' {X : Set} (h : X ≠ ∅) : ∃ x, x ∈ X := by
  -- Докажем от противного, предположим обратное.
  by_contra! h
  -- Чтобы получить противоречие, достаточно "столкнуть"
  -- гипотезу X ≠ ∅ с равенством X = ∅. А единственный известный способ
  -- доказать равенство множеств — это аксиома объёмности:
  -- X = Y ⟺ ∀ x, x ∈ X ↔ x ∈ Y.
  -- Отсюда естественно сформулировать промежуточное утверждение claim
  -- именно в такой форме (для Y = ∅), а не в какой-то другой.
  have claim (x : Object) : x ∈ X ↔ x ∈ (∅ : Set) := by
    -- Это утверждение доказывается "тривиально": обе стороны импликации по
    -- отдельности ложны (x ∈ X ложно по h, x ∈ ∅ ложно по not_mem_empty),
    -- так что каждое направление ⟹ закрывается своим собственным противоречием.
    specialize h x
    constructor
    · intro h'
      contradiction
    · intro h'
      have hc := not_mem_empty x -- ∀ x, x ∉ (∅ : Set)
      contradiction
  apply ext at claim -- (h : ∀ x, x ∈ X ↔ x ∈ Y) : X = Y
  contradiction

-- Вот это менее интуитивно понятное док-во из-за apply h.
lemma SetTheory.Set.nonempty_def'' {X : Set} (h : X ≠ ∅) : ∃ x, x ∈ X := by
  by_contra! hc
  apply h
  rw [eq_empty_iff_forall_notMem] -- X = ∅ ↔ ∀ (x : Object), x ∉ X
  exact hc

-- Утверждение: если некоторый объект `x` принадлежит множеству `X` (гипотеза `h`),
-- то `X` не может быть пустым множеством. Это обращение леммы `nonempty_def`:
-- там из `X ≠ ∅` выводится существование элемента, здесь наоборот — из
-- существования (конкретного) элемента `x ∈ X` выводится `X ≠ ∅`.
theorem SetTheory.Set.nonempty_of_inhabited
  {X : Set} {x : Object} (h : x ∈ X) : X ≠ ∅ := by
    -- Предположим обратноe: что множество `X` пустое.
    -- `contrapose!` это закон логической контрапозиции:
    -- `(P → Q) ⟺ (¬Q → ¬P)`.
    -- Тактика `contrapose!` заменяет цель `X ≠ ∅` (т.е. `X = ∅ → False`) и
    -- гипотезу `h : x ∈ X` на цель `x ∉ X` и гипотезу `h : X = ∅`,
    -- попутно проталкивая отрицания внутрь (`¬(a ≠ b) ⤳ a = b`).
    contrapose! h
    rw [eq_empty_iff_forall_notMem] at h --  X = ∅ ↔ ∀ (x : Object), x ∉ X
    specialize h x
    exact h

-- `Singleton` — это тайп-класс Lean/Mathlib, который даёт нотацию `{x}` для
-- одноэлементной коллекции: `class Singleton (α β) where singleton : α → β`.
-- Здесь мы связываем эту нотацию с уже определённой примитивной `singleton`
-- из аксиомы 3.4, чтобы можно было писать `{x} : Set` вместо `singleton x`.
instance SetTheory.Set.instSingleton : Singleton Object Set where
  singleton := singleton

-- Теперь можно использовать нотацию `{x}` для одноэлементного `Set`.
example (x : Object) : {x} = SetTheory.singleton x := rfl

/--
  Axiom 3.3(a) (одноэлементное множество).
  Здесь мы просто ссылаемся на нашу аксиому из класса типов SetTheory.

  Замечание: в некоторых случаях может понадобиться явно
  привести {lean (type := "Set")}`{a}` к {name}`Set` из-за
  уже существующей в Mathlib нотации теории множеств.
-/
@[simp]
theorem SetTheory.Set.mem_singleton (x a : Object) : x ∈ ({a} : Set) ↔ x = a :=
  singleton_axiom x a

-- Аналогично `Singleton` выше: `Union` — тайп-класс, дающий нотацию `X ∪ Y`.
-- Связываем её с примитивной `union_pair` из аксиомы 3.5,
-- чтобы не писать `union_pair X Y` каждый раз вручную.
instance SetTheory.Set.instUnion : Union Set where
  union := union_pair

-- Теперь можно использовать нотацию `X ∪ Y` для объединения двух `Set`.
example (X Y : Set) : X ∪ Y = SetTheory.union_pair X Y := rfl

/-- Axiom 3.4 (попарное объединение).
    Опять же, просто ссылаемся на нашу определенную выше аксиому. -/
@[simp]
theorem SetTheory.Set.mem_union (x : Object) (X Y : Set) :
  x ∈ (X ∪ Y) ↔ (x ∈ X ∨ x ∈ Y) :=
    union_pair_axiom X Y x

-- `Insert` — тайп-класс, дающий нотацию `insert x X`, а вместе с `Singleton`
-- ещё и нотацию `{a, b, c, ...}` для конечных кортежей
-- (она разворачивается как `insert a (insert b {c})` и т.д.).
-- В отличие от `Singleton` и `Union` выше, здесь нет
-- отдельной примитивной аксиомы для "вставки" элемента —
-- мы просто определяем её через уже имеющиеся `{x}` и `∪`:
-- добавить `x` к `X` значит взять объединение `X` с одноэлементным множеством `{x}`.
instance SetTheory.Set.instInsert : Insert Object Set where
  insert x X := {x} ∪ X

-- Множество, полученное вставкой элемента `b` в `X`, содержит
-- ровно те же элементы, что и `X`, плюс сам `b`.
-- Т.е. `a` попадает в `insert b X` тогда и только тогда,
-- когда `a` совпадает с только что вставленным `b`, либо `a` уже был элементом `X`.
--
-- Разворачиваем `insert b X` до `{b} ∪ X` (по определению `instInsert` выше),
-- после чего применяем уже доказанные `mem_union` и `mem_singleton`.
@[simp]
theorem SetTheory.Set.mem_insert (a b : Object) (X : Set) :
  a ∈ insert b X ↔ a = b ∨ a ∈ X := by
    simp only [insert, mem_union, mem_singleton]

-- Тот же результат, но без `simp`: каждый шаг здесь можно прощёлкать
-- в редакторе и увидеть, как именно меняются цель и гипотезы.
theorem SetTheory.Set.mem_insert' (a b : Object) (X : Set) :
  a ∈ insert b X ↔ a = b ∨ a ∈ X := by
    -- `insert b X` по определению `instInsert` — это `{b} ∪ X`
    -- `show` переписывает цель на определённо равную ей форму, не трогая доказательство.
    show a ∈ ({b} ∪ X) ↔ a = b ∨ a ∈ X
    constructor
    · intro h
      rw [mem_union] at h -- h : a ∈ {b} ∨ a ∈ X
      rcases h with h | h
      · left
        rw [mem_singleton] at h -- h : a = b
        exact h
      · right
        exact h
    · intro h
      rw [mem_union] -- цель : a ∈ {b} ∨ a ∈ X
      rcases h with h | h
      · left
        rw [mem_singleton] -- цель : a = b
        exact h
      · right
        exact h

/-- Axiom 3.3(b) (пара).
    Замечание: в некоторых случаях может понадобиться
    привести {lean (type := "Set")}`{a,b}` к {name}`Set`. -/
theorem SetTheory.Set.pair_eq (a b : Object) :
  ({a,b} : Set) = {a} ∪ {b} := by rfl

/-- Axiom 3.3(b) (пара).
    Замечание: в некоторых случаях может понадобиться
    привести {lean (type := "Set")}`{a,b}` к {name}`Set`. -/
@[simp]
theorem SetTheory.Set.mem_pair (x a b : Object) :
  x ∈ ({a,b} : Set) ↔ (x = a ∨ x = b) := by
    simp [pair_eq, mem_union, mem_singleton]

-- тот же результат, что и `mem_pair`, но без `simp` и пошагово
theorem SetTheory.Set.mem_pair' (x a b : Object) :
  x ∈ ({a,b} : Set) ↔ (x = a ∨ x = b) := by
    rw [pair_eq] -- {a, b} = {a} ∪ {b}
    rw [mem_union] -- x ∈ (X ∪ Y) ↔ (x ∈ X ∨ x ∈ Y)
    rw [mem_singleton, mem_singleton] -- x ∈ {a} ↔ x = a

-- Тройка `{a,b,c}` — это множество ровно из трёх
-- (не обязательно различных) элементов `a`, `b`, `c`.
-- Значит `x` попадает в `{a,b,c}` тогда и только тогда,
-- когда `x` совпадает хотя бы с одним из них.
@[simp]
theorem SetTheory.Set.mem_triple (x a b c : Object) :
  x ∈ ({a,b,c} : Set) ↔ (x = a ∨ x = b ∨ x = c) := by
    simp [insert, mem_union, mem_singleton]

-- Тот же результат, но без `simp` и пошагово.
theorem SetTheory.Set.mem_triple' (x a b c : Object) :
  x ∈ ({a,b,c} : Set) ↔ (x = a ∨ x = b ∨ x = c) := by
    -- {a,b,c} по определению instInsert равно {a} ∪ {b,c}.
    show x ∈ (({a} ∪ {b,c} : Set)) ↔ (x = a ∨ x = b ∨ x = c)
    constructor
    · intro h
      rw [mem_union] at h -- h : x ∈ {a} ∨ x ∈ {b,c}
      rcases h with h | h
      · left
        rw [mem_singleton] at h -- h : x = a
        exact h
      · right
        rw [mem_pair] at h -- h : x = b ∨ x = c
        exact h
    · intro h
      rw [mem_union] -- цель : x ∈ {a} ∨ x ∈ {b,c}
      rcases h with h | h
      · left
        rw [mem_singleton] -- цель : x = a
        exact h
      · right
        rw [mem_pair] -- цель : x = b ∨ x = c
        exact h

-- Множество, содержащее ровно элементы `a` и `b` (и ничего больше), определено
-- однозначно — существует ровно одно такое мн-во `X`.
-- Это оправдывает саму запись `{a,b}`:
-- мы вправе говорить "пара `a` и `b`",
-- поскольку такое множество не только существует, но и единственно.
/-- Remark 3.1.9 -/
theorem SetTheory.Set.pair_uniq (a b : Object) :
  ∃! (X : Set), ∀ x, x ∈ X ↔ x = a ∨ x = b := by
    apply existsUnique_of_exists_of_unique
    · use {a, b}
      intro x
      exact mem_pair x a b
    · intro s₁ s₂ h₁ h₂
      ext x
      -- rw [h₁ x, h₂ x]
      specialize h₁ x
      specialize h₂ x
      rw [h₁, h₂]

-- Аналогично `pair_uniq` выше, но для одноэлементного случая — множество,
-- содержащее ровно элемент `a` и ничего больше, единственно.
-- Это оправдывает запись `{a}` как обозначение конкретного,
-- вполне определённого множества.
/-- Remark 3.1.9 -/
theorem SetTheory.Set.singleton_uniq (a : Object) :
  ∃! (X : Set), ∀ x, x ∈ X ↔ x = a := by
    -- have haa : ∃! X, ∀ (x : Object), x ∈ X ↔ x = a ∨ x = a := pair_uniq a a
    -- simp only [or_self] at haa
    -- exact haa
    --
    -- Докажем существование уникального множества, доказав отдельно
    -- 1. Существование этого множества
    -- 2. Уникальность этого множества
    apply existsUnique_of_exists_of_unique
    · use {a}
      intro x
      exact mem_singleton x a
    · intro s₁ s₂ h₁ h₂
      ext x
      -- rw [h₁ x, h₂ x]
      specialize h₁ x
      specialize h₂ x
      rw [h₁, h₂]

-- Множество не хранит порядок элементов, поэтому
-- пара `{a,b}` и пара `{b,a}` — это одно и то же множество.
/-- Remark 3.1.9 -/
theorem SetTheory.Set.pair_comm (a b : Object) : ({a,b} : Set) = {b,a} := by
  ext x
  rw [mem_pair, mem_pair]
  rw [or_comm] -- a ∨ b ↔ b ∨ a

-- Множество не хранит кратность элементов: пара `{a,a}`, где элемент
-- повторён дважды, на самом деле состоит ровно из одного элемента и
-- совпадает с одноэлементным множеством `{a}`.
/-- Remark 3.1.9 -/
@[simp]
theorem SetTheory.Set.pair_self (a : Object) : ({a,a} : Set) = {a} := by
  ext x
  rw [mem_pair]
  rw [or_self] -- (p ∨ p) = p
  rw [mem_singleton]

-- Обратное к `pair_comm`/`pair_self`: раз пара как множество не различает
-- порядок и не хранит кратность, то из равенства `{a,b} = {c,d}` можно
-- заключить лишь то, что пары "сопоставлены" одним из двух способов —
-- `a` с `c` и `b` с `d`, либо `a` с `d` и `b` с `c` — а не обязательно
-- поэлементно в исходном порядке.
/-- Exercise 3.1.1 -/
theorem SetTheory.Set.pair_eq_pair {a b c d : Object}
  (h : ({a,b} : Set) = {c,d}) : (a = c ∧ b = d) ∨ (a = d ∧ b = c) := by
    rw [Set.ext_iff] at h
    have ha := h a
    have hb := h b
    have hc := h c
    have hd := h d
    rw [mem_pair, mem_pair] at ha hb hc hd
    simp at ha hb hc hd
    -- hc : c = a ∨ c = b,  hd : d = a ∨ d = b
    -- Разбираем 4 комбинации для ha и hb.
    -- Вырожденные случаи (a=c,b=c) и (a=d,b=d) требуют hd/hc,
    -- чтобы установить, что второй элемент пары тоже совпадает
    --
    -- `rcases ha with rfl | rfl` разбирает `ha : a = c ∨ a = d` на два случая;
    -- в каждом из них шаблон `rfl` (а не просто имя вроде `h`) заставляет
    -- rcases сразу вызвать `subst`: переменная `a` физически заменяется на
    -- `c` (или на `d`) везде в цели и контексте, а сама гипотеза `ha`
    -- исчезает — вместо равенства `a = c`, которое пришлось бы использовать
    -- вручную через `rw`, мы получаем контекст, где `a` больше не встречается.
    -- Комбинатор `<;>` затем в КАЖДОМ из этих двух случаев запускает
    -- `rcases hb with rfl | rfl`, аналогично разбирая и подставляя `b` —
    -- итого получаем 2 × 2 = 4 цели (по одной на каждый `·` ниже).
    rcases ha with rfl | rfl <;> rcases hb with rfl | rfl
    · rcases hd with rfl | rfl <;> left <;> exact ⟨rfl, rfl⟩
    · left; exact ⟨rfl, rfl⟩
    · right; exact ⟨rfl, rfl⟩
    · rcases hc with rfl | rfl <;> right <;> exact ⟨rfl, rfl⟩

-- `abbrev` — это просто «прозрачный» псевдоним (в отличие от `def`, Lean всегда
-- автоматически разворачивает его при проверке типов).

abbrev SetTheory.Set.empty : Set := ∅
abbrev SetTheory.Set.singleton_empty : Set := {(empty : Object)}
abbrev SetTheory.Set.pair_empty : Set := {(empty : Object), (singleton_empty : Object)}

-- Пустое множество не равно одноэлементному множеству из пустого.
/-- Exercise 3.1.2 (пустое множество не является одноэлементным) -/
theorem SetTheory.Set.emptyset_neq_singleton : empty ≠ singleton_empty := by
  intro h
  -- ^ По сути, рассуждаем от противного таким образом.
  rw [Set.ext_iff] at h
  specialize h empty
  obtain ⟨h₀, h₁⟩ := h
  -- h₁ : `empty ∈ singleton_empty → empty ∈ empty`.
  -- Заметим, что `empty ∈ empty` (`∅ ∈ ∅`) противоречит аксиоме 3.3:
  -- `not_mem_empty : ∀ x, x ∉ (∅ : Set)`, специализировав ее `∅` получим `∅ ∉ ∅`.
  -- Чтобы воспользоваться `h1` и получить `empty ∈ empty`, нужна посылка `hmem`.
  -- Она верна тривиально: `singleton_empty = {empty}`.
  have hmem : (empty : Object) ∈ singleton_empty := by
    -- singleton_empty = {(empty : Object)} по определению
    -- mem_singleton говорит: x ∈ {a} ↔ x = a
    -- После rw [mem_singleton]: (empty : Object) = (empty : Object),
    -- которая закрывается rfl автоматически
    -- Это верно просто по аксиоме 3.3(a):
    rw [mem_singleton]
  have hh : (empty : Object) ∈ empty := h₁ hmem
  rw [show empty = ∅ by rfl] at hh
  have hc := not_mem_empty (∅ : Set) -- Axiom 3.3 : (x : Object) : x ∉ ∅
  contradiction

-- Более короткая версия доказательства теоремы выше.
theorem SetTheory.Set.emptyset_neq_singleton' : empty ≠ singleton_empty := by
  intro h
  -- Axiom 3.3(a): x ∈ {a} ↔ x = a
  have hmem : (empty : Object) ∈ singleton_empty := by rw [mem_singleton]
  rw [← h] at hmem
  exact not_mem_empty _ hmem -- ∀ x, x ∉ (∅ : Set)

/-- Exercise 3.1.2 (пустое множество не является парой) -/
-- Пустое множество не равно 2-х элементному множеству из двух пустых.
theorem SetTheory.Set.emptyset_neq_pair : empty ≠ pair_empty := by
  intro h
  rw [Set.ext_iff] at h
  obtain ⟨h₀, h₁⟩ := h empty
  -- Доказательство аналогично доказательству выше для одноэлементного множества.
  have hmem : (empty : Object) ∈ pair_empty := by
    rw [mem_pair]
    left
    rfl
  have hh : (empty : Object) ∈ empty := h₁ hmem
  rw [show empty = ∅ by rfl] at hh
  have hc := not_mem_empty (∅ : Set) -- Axiom 3.3 : (x : Object) : x ∉ ∅
  contradiction

-- тот же результат, что и `emptyset_neq_pair`, но короче: `tauto` сам закрывает дизъюнкцию `x = a ∨ x = b`
theorem SetTheory.Set.emptyset_neq_pair' : empty ≠ pair_empty := by
  intro h
  -- Это утверждение получаем "бесплатно" из Axiom 3.3(b):
  have hmem : (empty : Object) ∈ pair_empty := by
    rw [mem_pair]
    tauto
  rw [← h, show empty = ∅ by rfl] at hmem
  have hnot := not_mem_empty (∅ : Set) -- Axiom 3.3 : (x : Object) : x ∉ ∅
  contradiction

-- Одноэлементное множество из пустого не равно двухэлементному множеству из пустых.
/-- Exercise 3.1.2 (одноэлементное не является парой) -/
theorem SetTheory.Set.singleton_empty_neq_pair : singleton_empty ≠ pair_empty := by
  intro h
  -- По аксиоме 3.3:
  have hmem : (singleton_empty : Object) ∈ pair_empty := by rw [mem_pair]; tauto
  rw [← h] at hmem
  rw [mem_singleton] at hmem -- x ∈ {a} ↔ x = a
  symm at hmem
  have heq : empty = singleton_empty := Set.coe_eq hmem
  have hnot := emptyset_neq_singleton
  contradiction

/--
  Remark 3.1.11.
  (Эти результаты можно доказать либо прямым переписыванием, либо через экстенсиональность)
-/
theorem SetTheory.Set.union_congr_left (A A' B : Set) (h : A = A') :
  A ∪ B = A' ∪ B := by rw [h]

/--
  Remark 3.1.11.
  (Эти результаты можно доказать либо прямым переписыванием, либо через экстенсиональность)
-/
theorem SetTheory.Set.union_congr_right (A B B' : Set) (h : B = B') :
  A ∪ B = A ∪ B' := by rw [h]

/-- Lemma 3.1.12 (базовые свойства объединений, одноэлементные) / Exercise 3.1.3 -/
theorem SetTheory.Set.singleton_union_singleton (a b : Object) :
  ({a} : Set) ∪ ({b} : Set) = {a,b} := by
    exact pair_eq a b -- {a, b} = {a} ∪ {b}

/-- Lemma 3.1.12 (базовые свойства объединений, коммутативность) / Exercise 3.1.3 -/
theorem SetTheory.Set.union_comm (A B : Set) : A ∪ B = B ∪ A := by
  ext x
  rw [mem_union, mem_union]
  tauto

/-- Lemma 3.1.12 (базовые свойства объединений, ассоциативность) / Exercise 3.1.3 -/
theorem SetTheory.Set.union_assoc (A B C : Set) : (A ∪ B) ∪ C = A ∪ (B ∪ C) := by
  -- Это доказательство написано так, чтобы следовать структуре оригинального текста.
  ext x
  constructor
  . intro hx
    rw [mem_union] at hx
    obtain case1 | case2 := hx
    . rw [mem_union] at case1
      obtain case1a | case1b := case1
      . rw [mem_union]; tauto
      · have : x ∈ B ∪ C := by rw [mem_union]; tauto
        rw [mem_union]; tauto
    · have : x ∈ B ∪ C := by rw [mem_union]; tauto
      rw [mem_union]; tauto
  · intro hx; rw [mem_union] at hx
    obtain case1 | case2 := hx
    · rw [mem_union, mem_union]
      tauto
    · rw [mem_union] at case2
      rw [mem_union, mem_union]
      tauto

/-- Proposition 3.1.27(c) (идемпотентность объединения). -/
@[simp]
theorem SetTheory.Set.union_self (A : Set) : A ∪ A = A := by
  ext x
  rw [mem_union]
  tauto

/-- Proposition 3.1.27(a) (объединение с пустым множеством). -/
@[simp]
theorem SetTheory.Set.union_empty (A : Set) : A ∪ ∅ = A := by
  ext x
  rw [mem_union]
  constructor
  · rintro (h₀ | h₁)
    · assumption
    · have hnot := not_mem_empty x
      contradiction
  · tauto

/-- Proposition 3.1.27(a) (пустое множество с объединением). -/
@[simp]
theorem SetTheory.Set.empty_union (A : Set) : ∅ ∪ A = A := by
  rw [union_comm]
  exact union_empty A

-- тройка `{a,b,c}` — это одноэлементное `{a}`, объединённое с парой `{b,c}`
theorem SetTheory.Set.triple_eq (a b c : Object) : {a,b,c} = ({a} : Set) ∪ {b,c} := by
  -- {a,b,c} = insert a {b,c} = {a} ∪ {b,c} по определению instInsert, поэтому rfl
  rfl

/-- Example 3.1.10 -/
theorem SetTheory.Set.pair_union_pair (a b c : Object) :
    ({a,b} : Set) ∪ {b,c} = {a,b,c} := by
  ext; simp only [mem_union, mem_pair, mem_triple]; tauto

-- тот же результат, что и `pair_union_pair`, но без `simp`: раскрываем `∈` через `rw` и явно разбираем дизъюнкцию
theorem SetTheory.Set.pair_union_pair' (a b c : Object) : ({a,b} : Set) ∪ {b,c} = {a,b,c} := by
  ext x
  rw [mem_union]
  rw [mem_pair, mem_pair]
  rw [mem_triple]
  -- tauto
  constructor <;> (intro h; tauto)

/-- Definition 3.1.14 (подмножество). -/
instance SetTheory.Set.instSubset : HasSubset Set where
  Subset X Y := ∀ x, x ∈ X → x ∈ Y

-- Теперь можно использовать `⊆` для отношения подмножества между двумя `Set`.
example (X Y : Set) : X ⊆ Y ↔ ∀ x, x ∈ X → x ∈ Y := by rfl

/--
  Definition 3.1.14 (строгое подмножество).
  Заметьте, что операция строгого подмножества в Mathlib обозначается {kw (of := «term_⊂_»)}`⊂`, а не `⊊`.
-/
instance SetTheory.Set.instSSubset : HasSSubset Set where
  SSubset X Y := X ⊆ Y ∧ X ≠ Y

/-- Теперь можно использовать {kw (of := «term_⊂_»)}`⊂` для
    отношения строгого подмножества между двумя {name}`Set`. -/
example (X Y : Set) : X ⊂ Y ↔ X ⊆ Y ∧ X ≠ Y := by rfl

-- разворачивает `⊆` в определение (Definition 3.1.14): `X ⊆ Y` — это `∀ x, x ∈ X → x ∈ Y`
theorem SetTheory.Set.subset_def (X Y : Set) :
  X ⊆ Y ↔ ∀ x, x ∈ X → x ∈ Y := by rfl

-- разворачивает `⊂` в определение: `X ⊂ Y` — это `X ⊆ Y` вместе с `X ≠ Y`
theorem SetTheory.Set.ssubset_def (X Y : Set) :
  X ⊂ Y ↔ (X ⊆ Y ∧ X ≠ Y) := by rfl

/-- Remark 3.1.15 -/
theorem SetTheory.Set.subset_congr_left
  {A A' B : Set} (hAA' : A = A') (hAB : A ⊆ B) : A' ⊆ B := by
    rw [← hAA']
    exact hAB

/-- Examples 3.1.16 (reflexivity) -/
@[simp, refl]
theorem SetTheory.Set.subset_self (A : Set) : A ⊆ A := by
  -- tauto
  rw [subset_def]
  intro x h
  exact h

/-- Examples 3.1.16 (the empty set is a subset) -/
@[simp]
theorem SetTheory.Set.empty_subset (A : Set) : ∅ ⊆ A := by
  rw [subset_def]
  intro x h₀
  have h₁ := not_mem_empty x
  contradiction

/-- Proposition 3.1.17
    Частичный порядок через включение множеств или мб даже лучше:
    Транзитивность операции включения множеств. -/
theorem SetTheory.Set.subset_trans
  {A B C : Set} (hAB : A ⊆ B) (hBC : B ⊆ C) : A ⊆ C := by
    -- Это доказательство написано так, чтобы следовать структуре оригинального текста.
    rw [subset_def]
    intro x hx
    rw [subset_def] at hAB
    -- Здесь `apply e at h` работает "в обратную сторону" по сравнению с `apply e` на цели:
    -- там мы унифицируем заключение `e` с целью, а посылки `e` становятся новыми целями.
    -- Здесь же `h` играет роль недостающей посылки:
    -- Lean подставляет `h` в качестве аргумента `e`,
    -- а получившееся заключение `e` записывается обратно в `h`, заменяя его.
    --
    -- То есть `apply (hAB x) at hx`, где `hAB x : x ∈ A → x ∈ B` и `hx : x ∈ A`,
    -- подставляет `hx` в качестве аргумента и
    -- кладёт результат `hAB x hx : x ∈ B` обратно в `hx`
    -- Это эквивалентно следующему: replace hx := hAB x hx
    apply (hAB x) at hx
    apply hBC x at hx
    assumption

/-- Proposition 3.1.17 (частичный порядок через включение множеств, антисимметричность) -/
theorem SetTheory.Set.subset_antisymm
  (A B : Set) (hAB : A ⊆ B) (hBA : B ⊆ A) : A = B := by
    ext x
    -- tauto
    rw [subset_def] at hAB hBA
    specialize hAB x
    specialize hBA x
    exact ⟨hAB, hBA⟩

/-- Proposition 3.1.17 (частичный порядок через включение множеств, строгая транзитивность) -/
theorem SetTheory.Set.ssubset_trans
  (A B C : Set) (hAB : A ⊂ B) (hBC : B ⊂ C) : A ⊂ C := by
    rw [ssubset_def] at *
    constructor
    · exact subset_trans hAB.left hBC.left
    · intro h
      rw [← h] at hBC
      have ha₀ := subset_antisymm A B hAB.left hBC.left
      have ha₁ := hAB.right
      contradiction
/-
  ## Подтип как «тип элементов множества»

  В теории типов Lean множество `A : Set` само по себе не является типом —
  нельзя написать `(x : A)` напрямую. `toSubtype` решает эту проблему:
  оно превращает множество в подтип объектов.

  Формально: `A.toSubtype = { x : Object // x ∈ A }`, то есть тип пар `⟨x, hx⟩`,
  где `x : Object` и `hx : x ∈ A`.
  Это «зависимая пара»: значение несёт в себе объект вместе с
  доказательством его принадлежности множеству.

  Создание элемента подтипа:
  · `⟨x, hx⟩ : A.toSubtype`  — анонимный конструктор
  · `A.subtype_mk hx`        — именованная вспомогательная функция

  Извлечение компонентов:
  · `x'.val      : Object`     — сам объект
  · `x'.property : x'.val ∈ A` — доказательство принадлежности

  Сокращённая запись через `CoeSort`:
  После объявления `instance : CoeSort Set (Type v)` Lean умеет автоматически
  считать `A : Set` типом (разворачивая до `A.toSubtype`).
  Поэтому вместо `(x' : A.toSubtype)` можно писать просто `(x' : A)`.

  Зачем это нужно:
  Аксиомы спецификации и замены принимают предикаты `P : A → Prop`
  (что означает `P : A.toSubtype → Prop`).
  Такой предикат получает не просто `Object`,
  а пару «объект + доказательство принадлежности»,
  что позволяет внутри `P` ссылаться на факт `x ∈ A` без лишних аргументов.
-/

/--
  Это определяет подтип {lean}`A.toSubtype` для любого {lean}`A:Set`.
  Заметьте, что {lean}`A.toSubtype` — это тип, точно так же,
  как {name}`Object` или {name}`Set` — типы.
  Значение {given (type := "A.toSubtype")}`x'` типа {lean}`A.toSubtype` объединяет
  некоторый {given}`x: Object` с доказательством того, что {given}`hx: x ∈ A`.

  Чтобы построить элемент {name}`x'` этого подтипа, используйте
  {lean (type := "A.toSubtype")}`⟨ x, hx ⟩`, где {lean}`x: Object` и {lean}`hx: x ∈ A`.
  Объект {name}`x`, связанный с элементом подтипа {name}`x'`, восстанавливается как
  {lean}`x'.val`, а свойство {name}`hx` того, что {name}`x` принадлежит {name}`A`,
  восстанавливается как {lean}`x'.property`.
-/
abbrev SetTheory.Set.toSubtype (A : Set) := Subtype (fun x ↦ x ∈ A)

-- A.toSubtype - Подмножество всех элементов множества А,
--               удовлетворяющих некоторому условию P.

example (A : Set) (x : Object) (hx : x ∈ A) : A.toSubtype := ⟨x, hx⟩
example (A : Set) (x' : A.toSubtype) : Object := x'.val
example (A : Set) (x' : A.toSubtype) : x'.val ∈ A := x'.property

-- На практике подтип позволяет упаковать объект вместе с
-- доказательством принадлежности в одно значение.
-- Сравни два доказательства: они эквивалентны, но второе упаковывает x и hx в x'.
example (A B : Set) (x : Object) (hx : x ∈ A) : x ∈ A ∪ B := by simp; left; exact hx
example (A B : Set) (x' : A.toSubtype) : x'.val ∈ A ∪ B := by simp; left; exact x'.property

instance : CoeSort (Set) (Type v) where
  coe A := A.toSubtype

-- Запись `x : A` означает, что x является элементом множества A.

-- Теперь благодаря инстансу CoeSort (Set) (Type v) вместо x : A.toSubtype можно писать просто x : A.
-- Сравни три доказательства: они эквивалентны, но последнее читается лаконичнее всего.
example (A B : Set) (x : Object) (hx : x ∈ A) : x ∈ A ∪ B := by simp; left; exact hx
example (A B : Set) (x' : A.toSubtype) : x'.val ∈ A ∪ B := by simp; left; exact x'.property
example (A B : Set) (x' : A) : x'.val ∈ A ∪ B := by simp; left; exact x'.property

/--
  Любой элемент множества (неявно приведённый к подтипу) принадлежит этому множеству
  в смысле отношения принадлежности данной теории множеств.
-/
lemma SetTheory.Set.subtype_property (A : Set) (x : A) : x.val ∈ A := x.property
-- обратное приведение `(x : Object)` элемента подтипа `A` совпадает с `x.val`
lemma SetTheory.Set.subtype_coe (A : Set) (x : A) : x.val = x := rfl
-- элементы подтипа `A` равны тогда и только тогда, когда равны их значения-объекты
lemma SetTheory.Set.coe_inj (A : Set) (x y : A) : x.val = y.val ↔ x = y := Subtype.coe_inj

/--
  Если есть доказательство {name}`hx` того, что {lean}`x ∈ A`, то {lean}`A.subtype_mk hx`
  построит элемент {name}`A` (рассматриваемого как подтип), соответствующий {name}`x`.
-/
def SetTheory.Set.subtype_mk (A : Set) {x : Object} (hx : x ∈ A) : A := ⟨x, hx⟩

-- Слева `A.subtype_mk hx : A.toSubtype`, справа `x : Object` — типы разные,
-- поэтому элаборатор вставляет коэрцию `↑`.
--
-- Это не `CoeSort (Set) (Type v)` выше, а другая,
-- встроенная в ядро Lean коэрция специально- для `Subtype`:
-- `instance subtypeCoe : CoeOut (Subtype p) α where coe v := v.val` (`Init/Coe.lean`).
--
-- Именно она и срабатывает здесь на *термах*:
-- `A.toSubtype` — это `Subtype (fun x ↦ x ∈ A)`,
-- так что элаборатор подставляет `↑ = Subtype.val`,
-- и на самом деле утверждается `(A.subtype_mk hx).val = x`.
--
-- А `A.subtype_mk hx` по определению — это `⟨x, hx⟩` (см. `subtype_mk` выше),
-- так что `.val` вычисляет ровно `x` уже на уровне определений
-- (iota-редукция проекции анонимного конструктора).
-- Отсюда равенство и закрывается `rfl`.
@[simp]
lemma SetTheory.Set.subtype_mk_coe {A : Set} {x : Object} (hx : x ∈ A) :
  A.subtype_mk hx = x := by rfl

/-
  ## Аксиома спецификации/выделения (Axiom 3.6): `A.specify P`

  `A.specify P` задумана как аналог `{ x ∈ A | P(x) }`, но в Lean она объявлена
  в классе `SetTheory` как **аксиома** — то есть `specify` создаёт некое непрозрачное
  множество, и Lean сам по себе не знает, что в нём лежит.

  Именно поэтому нужны три леммы ниже: они составляют API, который сообщает Lean'у
  правила принадлежности `A.specify P`. Без них о содержимом этого множества
  нельзя доказать ничего.

  Тип предиката: `P : A → Prop`, то есть `P : A.toSubtype → Prop`.
  Предикат принимает не просто `x : Object`, а элемент подтипа — пару ⟨x, hx⟩,
  где `hx : x ∈ A`. Благодаря этому внутри `P` автоматически известно,
  что аргумент принадлежит `A`.
-/
abbrev SetTheory.Set.specify (A : Set) (P : A → Prop) : Set :=
  SetTheory.specify A P

-- Три леммы ниже — это одна и та же аксиома 3.6, просто сформулированная по-разному
-- в зависимости от того, что уже есть под рукой в доказательстве:
--
-- specification_axiom   : x ∈ A.specify P → x ∈ A
-- specification_axiom'  : (x : A) → (x.val ∈ A.specify P ↔ P x)
-- specification_axiom'' : (x : Object) → (x ∈ A.specify P ↔ ∃ h : x ∈ A, P ⟨x, h⟩)

-- Вот эти три теоремы просто являются
-- спецификой реализации аксиомы спецификации (выделения) в Lean4.
-- Они нужны для инструментального удобства, скажем так :)

-- Направление «→»: принадлежность спецификации влечёт принадлежность исходному множеству.
-- Используй, когда имеешь `h : x ∈ A.specify P` и нужно получить `x ∈ A`.
/-- Axiom 3.6 (аксиома спецификации, принадлежность) -/
theorem SetTheory.Set.specification_axiom
  {A : Set} {P : A → Prop} {x : Object} (h : x ∈ A.specify P) : x ∈ A := by
    -- `SetTheory.specification_axiom A P` представляет собой конъюнкцию двух фактов:
    -- 1) `∀ x, x ∈ A.specify P → x ∈ A` (спецификация ⊆ A)
    -- 2) `∀ x, x.val ∈ A.specify P ↔ P x` (характеризация через P)
    -- Нам нужно направление ⊆, поэтому берём первый конъюнкт `.1`
    have hh := (SetTheory.specification_axiom A P).1
    exact hh x h

-- Двустороннее ↔, когда x уже является элементом подтипа `x : A`.
-- Используй, когда уже держишь в руках элемент подтипа и хочешь переключиться
-- между `x.val ∈ A.specify P` и `P x`.
/-- Axiom 3.6 (аксиома спецификации, предикат выполняется) -/
theorem SetTheory.Set.specification_axiom'
  {A : Set} (P : A → Prop) (x : A) : x.val ∈ A.specify P ↔ P x := by
    have hh := (SetTheory.specification_axiom A P).2
    exact hh x

-- В отличие от specification_axiom' выше, здесь x : Object —
-- просто объект, а не элемент подтипа A.
-- Это основной вариант для работы в доказательствах с `x : Object`.
/-- Axiom 3.6 (аксиома спецификации, выделенное множество — подмножество) -/
@[simp]
theorem SetTheory.Set.specification_axiom''
  {A : Set} (P : A → Prop) (x : Object) :
    x ∈ A.specify P ↔ ∃ h : x ∈ A, P ⟨x, h⟩ := by
      -- Разбиваем ↔ на два направления: `mp` (→) и `mpr` (←).
      constructor
      . -- из `h : x ∈ A.specify P` нужно построить `∃ h : x ∈ A, P ⟨x, h⟩`.
        intro h
        -- Доказательство `x ∈ A` можно сразу получить из `specification_axiom h` (см лемму выше).
        -- `use` подставляет (hxa : x ∈ A) как свидетеля существования.
        have hxa : x ∈ A := specification_axiom h -- (h : x ∈ A.specify P) : x ∈ A
        use hxa
        -- `specification_axiom' P : (y : A) → (y.val ∈ A.specify P ↔ P y)`.
        -- Переписывая цель `P ⟨x, hxa⟩` по этой эквивалентности (`←`),
        -- получаем `↑⟨x, hxa⟩.val ∈ A.specify P`,
        -- a по определению `.val` это есть просто `x ∈ A.specify P`,
        -- то есть в точности гипотеза `h`.
        rw [←specification_axiom' P]
        exact h
      -- Из `∃ h : x ∈ A, P ⟨x, h⟩` нужно получить `x ∈ A.specify P`.
      intro ⟨h, hP⟩
      -- Тем же переписыванием `←specification_axiom' P` превращаем
      -- `hP : P ⟨x, h⟩` в `⟨x, h⟩.val ∈ A.specify P`,
      -- то есть `x ∈ A.specify P` — это и есть цель.
      rw [←specification_axiom' P] at hP
      exact hP
      -- Ну, или проще:
      -- simpa [←specification_axiom' P] using hP

-- Непосредственное следствие specification_axiom: спецификация есть подмножество A.
-- Ну, по сути, это и есть выделение некоторого подмножества при помощи "фильтрации" по предикату.
theorem SetTheory.Set.specify_subset {A : Set} (P : A → Prop) : A.specify P ⊆ A := by
  rw [subset_def]
  intro x hx
  exact specification_axiom hx -- (h : x ∈ A.specify P) : x ∈ A

/--
  Одинаковые множества + Равносильные условия ⇒ Одинаковые выделенные подмножества.
  То есть `{x ∈ A | P x} = {x ∈ A' | P' x}`, если `A = A'` и `P`, `P'` согласны на общих x.

  Это `congr`-лемма для `specify`, но обычный `congr` тут не работает: `P : A.toSubtype → Prop`
  и `P' : A'.toSubtype → Prop` формально имеют разные типы (даже при `A = A'` типы `A.toSubtype`
  и `A'.toSubtype` не обязаны быть определённо равны), поэтому сравнивать `P` и `P'` напрямую
  нельзя — сравнение идёт поточечно, через исходный `x : Object` и отдельные доказательства
  принадлежности.

  Это упражнение может потребовать некоторого понимания того, как подтипы реализованы в Lean.
-/
theorem SetTheory.Set.specify_congr
  {A A' : Set} (hAA' : A = A') {P : A → Prop} {P' : A' → Prop}
    (hPP' : (x : Object) → (h : x ∈ A) → (h' : x ∈ A') → (P ⟨x, h⟩ ↔ P' ⟨x, h'⟩)) :
      A.specify P = A'.specify P' := by
        -- Равенство множеств доказывается через экстенсиональность
        ext x
        have h₀ := specification_axiom'' P  x -- (x : Object) : x ∈ A.specify P ↔ ∃ (h : x ∈ A), P ⟨x, h⟩
        have h₁ := specification_axiom'' P' x -- (x : Object) : x ∈ A.specify P ↔ ∃ (h : x ∈ A), P ⟨x, h⟩
        subst hAA' -- Заменим везде A' на A
        rw [h₀, h₁]
        constructor
        · rintro ⟨h, hP⟩
          have heq := hPP' x h h
          have hP' := heq.mp hP
          exact ⟨h, hP'⟩
        · rintro ⟨h, hP'⟩
          have heq := hPP' x h h
          have hP := heq.mpr hP'
          exact ⟨h, hP⟩

-- тот же результат, что и `specify_congr`, но `subst hAA'` выполняется сразу,
-- ещё до `ext`, а не после раскрытия `specification_axiom''`
theorem SetTheory.Set.specify_congr'
  {A A' : Set} (hAA' : A = A') {P : A → Prop} {P' : A' → Prop}
    (hPP' : (x : Object) → (h : x ∈ A) → (h' : x ∈ A') → P ⟨x, h⟩ ↔ P' ⟨x, h'⟩) :
      A.specify P = A'.specify P' := by
        -- Можно было бы сразу везде выполнить замену: A' = A
        subst hAA'
        -- Затем рассмотреть равенство множеств по экстенсиональности
        ext x
        -- simp only [specification_axiom'']
        -- (x : Object) : x ∈ A.specify P ↔ ∃ (h : x ∈ A), P ⟨x, h⟩
        rw [specification_axiom'', specification_axiom'']
        -- exists_congr (h : ∀ (a : α), p a ↔ q a) : (∃ a, p a) ↔ ∃ a, q a
        exact exists_congr fun h => hPP' x h h
        --
        -- exists_congr словами: если для любого из одного следует другое, то
        -- найдется хотя бы один, для которого это верно.
        -- constructor
        -- · rintro ⟨h, hP⟩
        --   have heq := hPP' x h h
        --   have hP' := heq.mp hP
        --   exact ⟨h, hP'⟩
        -- · rintro ⟨h, hP'⟩
        --   have heq := hPP' x h h
        --   have hP := heq.mpr hP'
        --   exact ⟨h, hP⟩

-- Определение пересечения с помощью инстанса класса типов Inter.
-- Нужно, чтобы иметь возможность использовать нотацию пересечения X ∩ Y.
instance SetTheory.Set.instIntersection : Inter Set where
  -- Любой элемент из X является также элементом из Y.
  -- Используем аксиому спецификации (выделения) для выделения такого подмножества из множества X.
  inter X Y := X.specify (fun x ↦ x.val ∈ Y)

-- Теперь можно использовать нотацию `X ∩ Y` для пересечения двух `Set`.
example (X Y : Set) : X ∩ Y = X.specify (fun x ↦ x.val ∈ Y) := rfl

/-- Definition 3.1.22 (пересечения) -/
@[simp]
theorem SetTheory.Set.mem_inter (x : Object) (X Y : Set) :
  x ∈ (X ∩ Y) ↔ (x ∈ X ∧ x ∈ Y) := by
    constructor
    · intro h
      -- `X ∩ Y` определено (см. `instIntersection` выше) как `X.specify (fun x ↦ x.val ∈ Y)`.
      -- Значит `h : x ∈ X ∩ Y` — это по определению `h : x ∈ X.specify P` для этого `P`.
      -- Применяя `specification_axiom : x ∈ A.specify P → x ∈ A` (направление ⊆) прямо к `h`,
      -- сразу получаем `x ∈ X` — без каких-либо дополнительных переписываний.
      have hX : x ∈ X := specification_axiom h
      -- (P : A → Prop) (x : A) : x.val ∈ A.specify P ↔ P x
      have hiff := specification_axiom' (fun z ↦ z.val ∈ Y) ⟨x, hX⟩
      have hY : x ∈ Y := hiff.mp h
      exact ⟨hX, hY⟩
    · rintro ⟨hX, hY⟩
      have hiff := specification_axiom' (fun z ↦ z.val ∈ Y) ⟨x, hX⟩
      exact hiff.mpr hY

/-- Definition 3.1.22 (пересечения) -/
@[simp]
theorem SetTheory.Set.mem_inter' (x : Object) (X Y : Set) :
  x ∈ (X ∩ Y) ↔ (x ∈ X ∧ x ∈ Y) := by
    constructor
    . intro h
      have h' := specification_axiom h
      simp [h']
      exact (specification_axiom' _ ⟨x, h'⟩).mp h
    · intro ⟨ hX, hY ⟩
      exact (specification_axiom' (fun x ↦ x.val ∈ Y) ⟨x, hX⟩).mpr hY

-- `SDiff` — тайп-класс, дающий нотацию `X \ Y`
-- (аналогично `Inter`/`instIntersection` выше для `∩`).
-- Разность множеств тоже уже выражается через `specify` —
-- это подмножество X из тех элементов, которые не лежат в Y.
instance SetTheory.Set.instSDiff : SDiff Set where
  sdiff X Y := X.specify (fun x ↦ x.val ∉ Y)

-- Теперь можно использовать нотацию `X \ Y` для разности двух `Set`.
example (X Y : Set) : X \ Y = X.specify (fun x ↦ x.val ∉ Y) := rfl

/-- Definition 3.1.26 (разностные множества) -/
@[simp]
theorem SetTheory.Set.mem_sdiff (x : Object) (X Y : Set) :
  x ∈ (X \ Y) ↔ (x ∈ X ∧ x ∉ Y) := by
    constructor
    . intro h
      -- (h : x ∈ A.specify P) : x ∈ A
      have h' := specification_axiom h
      simp [h']
      -- (P : A → Prop) (x : A) : x.val ∈ A.specify P ↔ P x
      have e := specification_axiom' (fun x ↦ x.val ∉ Y) ⟨x, h'⟩
      exact e.mp h
    · intro ⟨hX, hY⟩
      exact (specification_axiom' (fun x ↦ x.val ∉ Y) ⟨x, hX⟩).mpr hY

/-- Proposition 3.1.27(d) / Exercise 3.1.6 -/
theorem SetTheory.Set.inter_comm (A B : Set) : A ∩ B = B ∩ A := by
  ext x
  constructor
  · intro h
    rw [mem_inter] at *
    exact ⟨h.right, h.left⟩
  · intro h
    rw [mem_inter] at *
    exact ⟨h.right, h.left⟩

/-- Proposition 3.1.27(d) / Exercise 3.1.6 — идиоматичная версия -/
theorem SetTheory.Set.inter_comm' (A B : Set) : A ∩ B = B ∩ A := by
  ext x
  simp only [mem_inter]
  rw [And.comm]

/-- Proposition 3.1.27(b) (поглощение подмножеством). -/
theorem SetTheory.Set.subset_union {A X : Set} (hAX : A ⊆ X) : A ∪ X = X := by
  ext x
  rw [mem_union]
  rw [subset_def] at hAX
  rw [or_iff_right_iff_imp] -- (a ∨ b ↔ b) ↔ a → b
  apply hAX

/-- Proposition 3.1.27(b) (поглощение объединением). -/
theorem SetTheory.Set.union_subset {A X : Set} (hAX : A ⊆ X) : X ∪ A = X := by
  rw [union_comm]
  exact subset_union hAX

/-- Proposition 3.1.27(c) (идемпотентность пересечения). -/
@[simp]
theorem SetTheory.Set.inter_self (A : Set) : A ∩ A = A := by
  ext x
  rw [mem_inter]
  rw [and_self]

/-- Proposition 3.1.27(e) -/
theorem SetTheory.Set.inter_assoc (A B C : Set) :
  (A ∩ B) ∩ C = A ∩ (B ∩ C) := by
    ext x
    repeat rw [mem_inter]
    tauto

/-- Proposition 3.1.27(f) (пересечение дистрибутивно относительно объединения). -/
theorem  SetTheory.Set.inter_union_distrib_left (A B C : Set) :
  A ∩ (B ∪ C) = (A ∩ B) ∪ (A ∩ C) := by
    ext x
    -- simp only [mem_inter, mem_union]
    rw [mem_union]
    repeat rw [mem_inter]
    rw [mem_union]
    -- tauto
    constructor
    · rintro ⟨ha, hb | hc⟩
      · left;  exact ⟨ha, hb⟩
      · right; exact ⟨ha, hc⟩
    · rintro (⟨ha, hb⟩ | ⟨ha, hc⟩)
      · exact ⟨ha, Or.inl hb⟩
      · exact ⟨ha, Or.inr hc⟩

/-- Proposition 3.1.27(f) (объединение дистрибутивно относительно пересечения). -/
theorem  SetTheory.Set.union_inter_distrib_left (A B C : Set) :
  A ∪ (B ∩ C) = (A ∪ B) ∩ (A ∪ C) := by
    ext x
    simp only [mem_inter, mem_union]
    -- rw [mem_inter]
    -- repeat rw [mem_union]
    -- rw [mem_inter]
    tauto

/-- Proposition 3.1.27(f) -/
theorem SetTheory.Set.union_compl {A X : Set} (hAX : A ⊆ X) : A ∪ (X \ A) = X := by
  ext x
  -- simp only [mem_union, mem_sdiff]
  rw [mem_union, mem_sdiff]
  rw [subset_def] at hAX -- X ⊆ Y ↔ ∀ x ∈ X, x ∈ Y
  specialize hAX x
  tauto

/-- Proposition 3.1.27(f) -/
theorem SetTheory.Set.inter_compl {A X : Set} : A ∩ (X \ A) = ∅ := by
  ext x
  rw [mem_inter, mem_sdiff]
  have hmem := not_mem_empty x -- ∀ x, x ∉ (∅ : Set)
  tauto

/-- Proposition 3.1.27(g) (дополнение объединения). -/
theorem SetTheory.Set.compl_union {A B X : Set} : X \ (A ∪ B) = (X \ A) ∩ (X \ B) := by
  ext x
  rw [mem_inter]
  repeat rw [mem_sdiff]
  rw [mem_union]
  rw [not_or] -- ¬(p ∨ q) ↔ ¬p ∧ ¬q
  tauto

/-- Proposition 3.1.27(g) (дополнение пересечения). -/
theorem SetTheory.Set.compl_inter {A B X : Set} : X \ (A ∩ B) = (X \ A) ∪ (X \ B) := by
  ext x
  rw [mem_union]
  repeat rw [mem_sdiff]
  rw [mem_inter]
  rw [not_and] -- ¬(a ∧ b) ↔ a → ¬b
  tauto

/-- Делаем множествa дистрибутивными решетками.
    Теперь наши множества образуют дистрибутивную решётку. -/
instance SetTheory.Set.instDistribLattice : DistribLattice Set where
  le := (· ⊆ ·)
  le_refl : ∀ (A : Set), A ⊆ A := subset_self
  le_trans : ∀ (X A B : Set), X ⊆ A → A ⊆ B → X ⊆ B := fun _ _ _ ↦ subset_trans
  le_antisymm : ∀ (A B : Set), A ⊆ B → B ⊆ A → A = B := subset_antisymm
  inf := (· ∩ ·)
  sup := (· ∪ ·)
  le_sup_left : ∀ (a b : Set), a ⊆ a ∪ b := by
    intro a b x hx
    rw [mem_union]
    left
    exact hx
  le_sup_right : ∀ (a b : Set), b ⊆ a ∪ b := by
    intro a b x hx
    rw [mem_union]
    right
    exact hx
  sup_le : ∀ (a b c : Set), a ⊆ c → b ⊆ c → a ∪ b ⊆ c := by
    intro a b c hac hbc x hx
    rw [mem_union] at hx
    -- tauto
    rcases hx with ha | hb
    · exact hac x ha
    · exact hbc x hb
  inf_le_left : ∀ (a b : Set), a ∩ b ⊆ a := by
    intro a b x hxab
    rw [mem_inter] at hxab
    exact hxab.1
  inf_le_right : ∀ (a b : Set), a ∩ b ⊆ b := by
    intro a b x hxab
    rw [mem_inter] at hxab
    exact hxab.2
  le_inf : ∀ (a b c : Set), a ⊆ b → a ⊆ c → a ⊆ b ∩ c := by
    intro a b c hab hac x hx
    rw [mem_inter]
    have hax := hab x hx
    have hcx := hac x hx
    exact ⟨hax, hcx⟩
  le_sup_inf : ∀ (x y z : Set), (x ∪ y) ∩ (x ∪ z) ⊆ x ∪ (y ∩ z) := by
    intro X Y Z
    rw [← union_inter_distrib_left] -- A ∪ (B ∩ C) = (A ∪ B) ∩ (A ∪ C)

/-- У множеств есть наименьший элемент.  -/
-- Даём Set структуру решётки через ⊆ как ≤, и ∅ как ⊥.
instance SetTheory.Set.instOrderBot : OrderBot Set where
  bot_le : ∀ (A : Set), ∅ ⊆ A := empty_subset
  bot := ∅

-- Disjoint (что-либо "непересекающееся") в Mathlib — это
-- общее понятие из теории решёток, а не специфичное для множеств.
-- Оно определено для любого типа с частичным порядком ≤ и наименьшим элементом ⊥:
--    Disjoint a b ↔ ∀ c, c ≤ a → c ≤ b → c ≤ ⊥
--
-- то есть единственный элемент, который лежит "ниже" одновременно a и b это ⊥.
-- Нам это нужно, чтобы не изобретать своё собственное определение непересекающихся множеств:
-- вместо этого мы даём Set структуру решётки (через ⊆ как ≤, и ∅ как ⊥, см. instOrderBot выше),
-- и тогда Mathlib-евское общее определение Disjoint автоматически
-- применимо к нашим множествам и совпадает с привычным A ∩ B = ∅
-- (это показано ниже в disjoint_iff).
--
-- Теперь мы определили `A ≤ B` как `A ⊆ B`, а `⊥` как `∅`.
-- Это заставляет определение `Disjoint` из Mathlib работать с нашим `Set`.
example (A B : Set) : (A ≤ B) ↔ (A ⊆ B) := by rfl
example : ⊥ = (∅ : Set) := by rfl
example (A B : Set) : Prop := Disjoint A B

/-- Определение непересекаемости (через предыдущие инстансы) -/
theorem SetTheory.Set.disjoint_iff (A B : Set) : Disjoint A B ↔ A ∩ B = ∅ := by
  -- `_root_.disjoint_iff : Disjoint a b ↔ a ⊓ b = ⊥` —
  -- общая лемма для любой решётки с ⊥ (`SemilatticeInf` + `OrderBot`).
  -- Наша цель `Disjoint A B ↔ A ∩ B = ∅` — это она же,
  -- только записанная через `∩`/`∅` вместо `⊓`/`⊥`: по `instDistribLattice`
  -- выше `⊓ := (· ∩ ·)`, а по `instOrderBot` `⊥ := ∅`,
  -- так что `A ⊓ B` и `A ∩ B`, как и `⊥` и `∅`, определённо равны
  -- (просто разные обозначения одного и того же терма).
  -- Поэтому `exact` проходит напрямую:
  -- при финальной проверке типов Lean разворачивает эти instance-поля и видит,
  -- что тип `_root_.disjoint_iff` совпадает с целью.
  exact _root_.disjoint_iff

-- Замена/подстановка (Axiom 3.7).
--
-- Смысл тот же, что у `{f(x) : x ∈ A}` для какой-то функции `f`,
-- только вместо явной `f` мы даём отношение `P` (`P x y` читается как "f(x) = y"),
-- а `hP` гарантирует, что за этим отношением на самом деле стоит функция,
-- тк этo гипотеза однозначности/функциональности
-- (см. пример с `{3,5,9}.replace` ниже по файлу).
-- `A.replace hP` — это множество всех "выходов" `P` по всем `x ∈ A`.
abbrev SetTheory.Set.replace (A : Set) {P : A → Object → Prop}
  (hP : ∀ x y y', P x y ∧ P x y' → y = y') : Set :=
    SetTheory.replace A P hP

/-- Axiom 3.7 (аксиома замены) -/
-- Позволяет задавать отображения типа таких {3,4,5} → {3++,4++,5++}.
-- Для этого берём P x y := "y = x++"
-- то есть P x y := ∃ n, x.val = n ∧ y = n+1,
-- а условие hP выполняется автоматически, потому что x++ определено однозначно.
-- Для примера см. "Example 3.1.30" ниже по файлу.
@[simp]
theorem SetTheory.Set.replacement_axiom {A : Set} {P : A → Object → Prop}
  (hP : ∀ x y y', P x y ∧ P x y' → y = y') (y : Object) :
    y ∈ A.replace hP ↔ ∃ x, P x y :=
      SetTheory.replacement_axiom A P hP y

abbrev Nat := SetTheory.nat

-- Далее мы будем использовать `Nat` как тип.
-- Однако, мы определили `Nat` как `SetTheory.nat`, а это `Set`, а не тип.
-- Единственная причина, по которой можно писать `x: Nat`, —
-- это ранее определённое приведение `CoeSort`, позволяющее писать
-- `x: A` (когда `A` — это `Set`) как сокращение для `x: A.toSubtype`.
-- Именно поэтому всякий раз, когда мы видим `x: Nat`,
-- на самом деле это `x: Nat.toSubtype`.
example (x : Nat) : Nat.toSubtype := x
example (x : Nat) : Object := x.val
example (x : Nat) : (x.val ∈ Nat) := x.property
example (o : Object) (ho : o ∈ Nat) : Nat := ⟨o, ho⟩

/-- Axiom 3.8 (аксиома бесконечности):
    Существует множество ℕ, элементы которого называются натуральными числами,
    а также объект 0 в n и объект n++, соответствующий каждому
    такому натуральному числу n из ℕ, для которого аксиомы Пeанo справедливы.
-/
def SetTheory.Set.nat_equiv : ℕ ≃ Nat := SetTheory.nat_equiv

-- Ниже — небольшое API для работы с приведениями типов.
-- Возможно, не самый оптимальный способ это устроить.

instance SetTheory.Set.instOfNat {n : ℕ} : OfNat Nat n where
  ofNat := nat_equiv n

-- Теперь можно задавать `Nat` натуральным литералом.
example : Nat := 5
example : (5 : Nat).val ∈ Nat := (5 : Nat).property

-- Добавление этого инстанса позволяет выполнять приведениe типов:
-- ℕ → Nat
instance SetTheory.Set.instNatCast : NatCast Nat where
  natCast n := nat_equiv n

-- Теперь можно превращать `ℕ` в `Nat`.
example (n : ℕ) : Nat := n
example (n : ℕ) : (n : Nat).val ∈ Nat := (n : Nat).property

-- Обратное направление к `instNatCast` выше:
-- `nat_equiv : ℕ ≃ Nat`, значит `nat_equiv.symm : Nat ≃ ℕ`.
-- Этот инстанс регистрирует `nat_equiv.symm` как то,
-- во что превращается `(n : ℕ)` для `n : Nat` —
-- именно эта функция стоит за `↑` в любом месте кода, где `Nat` приводится к `ℕ`.
instance SetTheory.Set.toNat : Coe Nat ℕ where
  coe n := nat_equiv.symm n

-- Теперь можно превращать `Nat` в `ℕ`.
example (n : Nat) : ℕ := n

instance SetTheory.Object.instNatCast : NatCast Object where
  natCast n := (n : Nat).val

-- Теперь можно превращать `ℕ` в `Object`.
example (n : ℕ) : Object := n
example (n : ℕ) : Set := {(n : Object)}

instance SetTheory.Object.instOfNat {n : ℕ} : OfNat Object n where
  ofNat := ((n : Nat) : Object)

-- Теперь можно задавать `Object` натуральным литералом.
example : Object := 1
example : Set := {1, 2, 3}

-- Число `n : ℕ` можно превратить в `Object` двумя способами:
-- напрямую `(n : Object)` или через `Nat` (`(n : Nat) : Object`).
-- Лемма говорит, что оба способа дают один и тот же `Object`.
@[simp]
lemma SetTheory.Object.ofnat_eq {n : ℕ} : ((n : Nat) : Object) = (n : Object) := rfl

-- То же равенство, что выше, но слева — числовой литерал `ofNat(n)`
-- (то, как пишется число `n` в виде `Object`, например `(3 : Object)`),
-- а не явное приведение `(n : Object)`.
lemma SetTheory.Object.ofnat_eq' {n : ℕ} : (ofNat(n) : Object) = (n : Object) := rfl

-- Если элемент `n : Nat` сначала перевести в обычное число `ℕ`
-- (это и есть `(n : ℕ)`, через `nat_equiv`), а затем это число — в `Object`,
-- получится то же самое, что перевести `n : Nat` в `Object` напрямую.
@[simp]
lemma SetTheory.Object.ofnat_eq'' {n : Nat} : ((n : ℕ) : Object) = (n : Object) := by
  simp [Nat.cast, NatCast.natCast, Equiv.apply_symm_apply]

-- `⟨(n : Object), hn⟩ : nat` — это число `n`,
-- представленное как элемент множества `nat` (`hn` доказывает, что оно ему принадлежит).
-- Если такой элемент перевести обратно в `ℕ`, получится то же самое `n` —
-- представление в виде элемента `nat` не теряет информацию о числе.
@[simp]
lemma SetTheory.Object.ofnat_eq''' {n : ℕ} {hn} : ((⟨(n : Object), hn⟩ : nat) : ℕ) = n := by
  simp [Nat.cast, NatCast.natCast, Equiv.symm_apply_apply]

-- Приведение `(n : Nat)` (через `NatCast`) и числовой литерал `OfNat.ofNat n` для `Nat` —
-- это одно и то же число, просто записанное двумя разными способами.
lemma SetTheory.Set.nat_coe_eq {n : ℕ} : (n : Nat) = OfNat.ofNat n := rfl

-- Приведение `ℕ → Nat` не путает разные числа:
-- `(n : Nat) = (m : Nat)` в точности когда `n = m`.
@[simp]
lemma SetTheory.Set.nat_equiv_inj (n m : ℕ) : (n : Nat) = (m : Nat) ↔ n = m  :=
  Equiv.apply_eq_iff_eq nat_equiv

-- То же самое в обратную сторону: приведение `Nat → ℕ` тоже не путает разные числа.
@[simp]
lemma SetTheory.Set.nat_equiv_symm_inj (n m : Nat) : (n : ℕ) = (m : ℕ) ↔ n = m :=
  Equiv.apply_eq_iff_eq nat_equiv.symm

-- Позволяет сравнивать числовые литералы `Nat` (например, `(5 : Nat) = (3 : Nat)`) через
-- равенство обычных чисел `ℕ` — этим пользуются тактики вроде `norm_num`/`decide`.
@[simp]
theorem SetTheory.Set.ofNat_inj (n m : ℕ) :
  (ofNat(n) : Nat) = (ofNat(m) : Nat) ↔ ofNat(n) = ofNat(m) := by
    exact nat_equiv_inj _ _

example : (5 : Nat) ≠ (3 : Nat) := by simp

-- то же самое, что и `ofNat_inj`, но для литералов, приведённых до уровня `Object`
@[simp]
theorem SetTheory.Set.ofNat_inj' (n m : ℕ) :
  (ofNat(n) : Object) = (ofNat(m) : Object) ↔ ofNat(n) = ofNat(m) := by
    simp only [←Object.ofnat_eq, Object.ofnat_eq', Set.coe_inj, Set.nat_equiv_inj]
    rfl

example : (5 : Object) ≠ (3 : Object) := by simp

-- Позволяет сравнивать приведение `(m : Object)` переменной `m : ℕ` с числовым литералом
-- `ofNat(n) : Object`, сводя такое равенство к обычному равенству чисел `m = n` — это и
-- нужно, чтобы `simp` решал цели вроде примера ниже (`(n : Object) = 2 ↔ n = 2`).
@[simp]
lemma SetTheory.Set.nat_coe_eq_iff {m n : ℕ} :
  (m : Object) = ofNat(n) ↔ m = n := by
    exact ofNat_inj' m n

example (n : ℕ) : (n : Object) = 2 ↔ n = 2 := by simp

-- Инъективность приведения `ℕ → Object` в общем виде:
-- в отличие от `nat_coe_eq_iff`, здесь обе стороны —
-- произвольные переменные `n`, `m : ℕ`, а не число и литерал.
@[simp]
theorem SetTheory.Object.natCast_inj (n m : ℕ) :
    (n : Object) = (m : Object) ↔ n = m := by
      simp [←ofnat_eq, Subtype.val_inj]

-- Если число `n : ℕ` перевести в `Nat`, а затем обратно в `ℕ`,
-- получится то же самое `n` — приведение туда и обратно не теряет и не искажает число.
@[simp]
lemma SetTheory.Set.nat_equiv_coe_of_coe (n : ℕ) : ((n : Nat) : ℕ) = n :=
  Equiv.symm_apply_apply nat_equiv n

-- То же самое в обратную сторону: элемент `n : Nat`,
-- переведённый в `ℕ` и обратно в `Nat`, совпадает с исходным `n`.
@[simp]
lemma SetTheory.Set.nat_equiv_coe_of_coe' (n : Nat) : ((n : ℕ) : Nat) = n :=
  Equiv.symm_apply_apply nat_equiv.symm n

-- Тот же факт «туда и обратно», что и `nat_equiv_coe_of_coe`, но для числового литерала
-- `ofNat(n) : Nat`, а не для приведения переменной: литерал, переведённый в `ℕ`, даёт `n`.
@[simp]
lemma SetTheory.Set.nat_equiv_coe_of_coe'' (n : ℕ) : ((ofNat(n) : Nat) : ℕ) = n :=
  nat_equiv_coe_of_coe n

-- Позволяет сравнивать в `Object` элемент `m : Nat`
-- с числовым литералом `ofNat(n) : Object`,
-- сведя это равенство к сравнению обычных чисел `(m : ℕ)` и `n` —
-- то есть даёт возможность работать с `Nat`-элементами и `ℕ`-литералами единообразно.
@[simp]
lemma SetTheory.Set.nat_coe_eq_iff' {m : Nat} {n : ℕ} :
  (m : Object) = (ofNat(n) : Object) ↔ (m : ℕ) = ofNat(n) := by
    constructor <;> intro h <;> rw [show m = n by aesop]
    apply nat_equiv_coe_of_coe; rfl

example : (2 : ℕ) ≠ (7 : Nat) := by simp

/-- Example 3.1.16 (упрощённая версия).  -/
example : ({3, 5} : Set) ⊆ {1, 3, 5} := by
  rw [subset_def]
  intro x
  rw [mem_pair]
  rw [mem_triple]
  tauto

/-- Example 3.1.17 (упрощённая версия). -/
example : ({3, 5} : Set).specify (fun x ↦ x.val ≠ 3) = ({5} : Set) := by
  ext x
  rw [mem_singleton]
  -- (P) (x) : x ∈ A.specify P ↔ ∃ (h : x ∈ A), P ⟨x, h⟩
  rw [specification_axiom'']
  constructor
  · rintro ⟨h1, h2⟩
    have h3 := (mem_pair x 3 5).mp h1 -- x ∈ {a, b} → x = a ∨ x = b
    rcases h3 with h3 | h5
    · have hx3 : x ≠ 3 := h2
      contradiction -- exact absurd h3 hx3
    · exact h5
  · rintro h
    rw [h]
    have hmem : (5 : Object) ∈ ({3, 5} : Set) :=
      (mem_pair 5 3 5).mpr (Or.inr rfl)
    use hmem
    show (5 : Object) ≠ 3
    norm_num

-- Пример использования norm_num, который мы будем использовать в упражнении ниже:
-- Ложная гипотеза о числовых литералах закрывает любую цель.
--
-- Как именно `ofNat_inj'` в этом участвует:
--
-- Это `@[simp]`-лемма, а `norm_num` внутри себя вызывает `simp`.
-- Поэтому `norm_num at h`, применённый к `h : (1 : Object) = 3`,
-- сначала переписывает `h` по `ofNat_inj'` в равенство обычных чисел `(1 : ℕ) = 3`,
-- а затем встроенный числовой движок `norm_num` видит, что `1 ≠ 3`,
-- и сводит `h` к `False`. Гипотеза `False` в контексте закрывает
-- любую цель автоматически — саму цель разбирать уже не нужно.
example (h : (1 : Object) = 3) : (1 : Object) = 2 ∨ (1 : Object) = 4 := by
  norm_num at h

/-- Example 3.1.24 -/
example : ({1, 2, 4} : Set) ∩ {2, 3, 4} = {2, 4} := by
  ext x
  rw [mem_inter]
  rw [mem_pair, mem_triple, mem_triple]
  -- `tauto` сам по себе не видит, что разные числовые литералы (Object) различны,
  -- поэтому в прямую сторону перебираем все 3×3 варианта равенства x и подставляем (subst h1).
  --
  -- В противоречивых случаях, например:
  -- h2 : (1 : Object) = 3
  -- -| 1 = 2 ∨ 1 = 4
  -- norm_num at h2 переписывает h2 через @[simp]-лемму ofNat_inj' в
  -- ℕ-равенство (1 : ℕ) = 3, встроенным числовым движком видит, что оно ложно,
  -- и сводит h2 к `False`. Гипотеза `False` в контексте автоматически закрывает
  -- текущую цель — саму дизъюнкцию `1 = 2 ∨ 1 = 4` доказывать не нужно.
  constructor
  · rintro ⟨h1 | h1 | h1, h2 | h2 | h2⟩ <;> subst h1 <;> first | tauto | norm_num at h2
  · rintro (h | h) <;> tauto

/-- Example 3.1.24 -/
example : ({1, 2, 4} : Set) ∩ {2, 3, 4} = {2, 4} := by
  ext x
  -- Вместо того, чтобы, как раньше, вручную разбирать повторяющиеся случаи,
  -- можно использовать тактику `aesop`, которая делает это автоматически.
  aesop -- Большой атом очевидности : )

/-- Example 3.1.25 -/
example : ({1, 2} : Set) ∩ {3, 4} = ∅ := by
  rw [eq_empty_iff_forall_notMem] -- X = ∅ ↔ ∀ (x : Object), x ∉ X
  aesop

-- Disjoint A B (см. пояснение выше про Mathlib-евское определение через решётки) —
-- это способ сказать «A и B не пересекаются», не привязываясь к
-- конкретным ∩ и ∅ (идее/операции пересечения и понятия пустоты или небытия).
--
-- Здесь мы хотим показать, что {1,2,3} и {2,3,4} пересекаются (у них общие элементы 2 и 3),
-- поэтому доказываем отрицание `¬ Disjoint ...`. Чтобы работать с этим определением,
-- сначала через `disjoint_iff` переписываем его в привычный вид `A ∩ B = ∅`, а дальше
-- `intro h` берёт это равенство как гипотезу (для доказательства отрицания). После
-- переписывания `h` в `notMem`-форму (`∀ x, x ∉ A ∩ B`) `aesop` сам подбирает `x = 2`,
-- проверяет по определению `∩`/`{..}`, что `2 ∈ A` и `2 ∈ B`, и тем самым противоречит `h`.
example : ¬ Disjoint ({1, 2, 3} : Set) {2, 3, 4} := by
  rw [disjoint_iff] -- Disjoint A B ↔ A ∩ B = ∅
  intro h
  rw [eq_empty_iff_forall_notMem] at h -- X = ∅ ↔ ∀ (x : Object), x ∉ X
  -- Методом "очевидности" (ниже — доказательство расписанное вручную, шаг за шагом)
  aesop

-- Методом "откуда это берётся?": вместо того, чтобы доверять `aesop`,
-- явно предъявляем свидетеля `x = 2` и проверяем,
-- что `h 2 : 2 ∉ A ∩ B` противоречит тому, что `2 ∈ A ∩ B`
-- (поскольку `2` — общий элемент `{1,2,3}` и `{2,3,4}`).
example : ¬ Disjoint ({1, 2, 3} : Set) {2, 3, 4} := by
  rw [disjoint_iff] -- Disjoint A B ↔ A ∩ B = ∅
  intro h
  rw [eq_empty_iff_forall_notMem] at h -- X = ∅ ↔ ∀ (x : Object), x ∉ X
  apply h 2
  rw [mem_inter] -- x ∈ X ∩ Y ↔ x ∈ X ∧ x ∈ Y
  -- refine — это "exact с дырками":
  -- принимает терм с плейсхолдерами ?_ или ?name,
  -- каждый плейсхолдер становится отдельной подцелью
  refine ⟨?h2in123, ?h2in234⟩
  · -- x ∈ {1, 2, 3} ↔ x = 1 ∨ x = 2 ∨ x = 3
    exact (mem_triple 2 /- ∈ -/ 1 2 3).mpr (Or.inr (Or.inl rfl))
  · exact (mem_triple 2 /- ∈ -/ 2 3 4).mpr (Or.inl rfl)

-- Методом очевидности.
example : Disjoint (∅ : Set) ∅ := by aesop

-- Методом "откуда это берётся?"
example : Disjoint (∅ : Set) ∅ := by
  rw [disjoint_iff] -- Disjoint A B ↔ A ∩ B = ∅
  rw [eq_empty_iff_forall_notMem] -- X = ∅ ↔ ∀ (x : Object), x ∉ X
  intro x hx
  rw [mem_inter] at hx -- x ∈ X ∩ Y ↔ x ∈ X ∧ x ∈ Y
  have h₀ := hx.1
  have h₁ := not_mem_empty x -- ∀ x, x ∉ (∅ : Set)
  contradiction

/-- Example 3.1.28 (разностные множества). -/
example : ({1, 2, 3, 4} : Set) \ {2, 4, 6} = {1, 3} := by
  apply ext; aesop

example : ({1, 2, 3, 4} : Set) \ {2, 4, 6} = {1, 3} := by
  ext x
  rw [mem_sdiff] -- (x ∈ X \ Y) ↔ x ∈ X ∧ x ∉ Y
  rw [mem_pair]  --  x ∈ {a, b} ↔ x = a ∨ x = b
  rw [mem_insert, mem_triple, mem_triple]
  -- Просто разбираем все случаи и проверяем каждый.
  constructor
  · rintro ⟨h1 | h1 | h1 | h1, h2⟩ <;> subst h1 <;> tauto
  · rintro (h | h) <;> subst h <;> exact ⟨by tauto, by norm_num⟩

/-
  Мы уже накопили некоторое количество аксиом и результатов о множествах,
  но всё ещё есть много вещей, которые мы пока не можем делать.

  Одна из базовых вещей, которую мы хотим делать с множеством —
  это взять каждый объект этого множества и каким-то образом
  преобразовать каждый такой объект в новый объект.

  Например, мы можем захотеть начать с множества чисел,
  скажем {3,5,9}, и увеличить каждое на единицу, получив новое множество {4,6,10}.

  Это не то, что мы можем сделать непосредственно,
  используя только уже имеющиеся аксиомы, поэтому нам нужна новая аксиома:

  Аксиома 3.6 (Замена):

  Пусть A — множество.
  Для любого объекта x ∈ A и любого объекта y предположим, что
  у нас есть утверждение P(x,y), относящееся к x и y, такое что
  для каждого x ∈ A существует не более одного y, для которого P(x,y) истинно.
  Тогда существует множество:
    {y : P(x,y) истинно для некоторого x ∈ A}, такое что для любого объекта z,
    z ∈ {y : P(x,y) истинно для некоторого x ∈ A} ⇐⇒ P(x,z) истинно для некоторого x ∈ A.

  Пояснение к формулировке выше:
  `y` внутри `{y : ...}` — связанная переменная, у неё нет смысла вне фигурных скобок.
  Поэтому переход от `{y : P(x,y) для некоторого x ∈ A}`
  к условию `P(x,z) для некоторого x ∈ A` — это подстановка:
  чтобы проверить, лежит ли конкретный объект `z` в этом множестве,
  мы просто подставляем `z` везде, где стояло `y`, в предикат `P`.
-/
/-- Example 3.1.30 -/
example : ({3, 5, 9} : Set).replace
  (P := fun x y ↦
    ∃ (n : ℕ), (x.val = n) ∧ (y = (n + 1 : ℕ))) (by aesop) = {4, 6, 10} := by
  ext x
  -- replacement_axiom:
  --   (hP : ∀ (x : A.toSubtype) (y y' : Object), P x y ∧ P x y' → y = y')
  --   (y : Object) :
  --   y ∈ A.replace hP ↔ ∃ x, P x y
  rw [replacement_axiom]
  rw [mem_triple]
  constructor
  · rintro ⟨x1, n, hn, hx⟩
    have h3 := x1.property
    rw [mem_triple] at h3
    rw [hn] at h3
    simp only [nat_coe_eq_iff] at h3
    rcases h3 with rfl | rfl | rfl
    · rw [hx]; left; norm_num
    · rw [hx]; right; left; norm_num
    · rw [hx]; right; right; norm_num
  · -- show x = 4 ∨ x = 6 ∨ x = 10 → ∃ x_1 n, ↑x_1 = ↑n ∧ x = ↑(n + 1)
    rintro (rfl | rfl | rfl)
    · have h3 : (3 : Object) ∈ ({3, 5, 9} : Set) := by
        -- simp
        rw [mem_triple]; left; rfl
      use ⟨3, h3⟩, 3
      simp
    · have h5 : (5 : Object) ∈ ({3, 5, 9} : Set) := by simp
      use ⟨5, h5⟩, 5
      simp
    · have h9 : (9 : Object) ∈ ({3, 5, 9} : Set) := by simp
      use ⟨9, h9⟩, 9
      simp

/-- Example 3.1.31 -/
example : ({3, 5, 9} : Set).replace (P := fun _ y ↦ y = 1) (by aesop) = {1} := by
  ext x
  -- aesop
  --
  -- replacement_axiom
  --   (hP : ∀ (x : A.toSubtype) (y y' : Object), P x y ∧ P x y' → y = y')
  --   (y : Object)
  --   :
  --   y ∈ A.replace hP ↔ ∃ x, P x y
  --
  -- replace: (hP : ∀ x y y', P x y ∧ P x y' → y = y') : Set
  --
  -- Фишка в том, что `{3, 5, 9}.replace` это и есть `hP`,
  -- то есть совпадает по типу с требуемой гипотезой для replacement_axiom.
  rw [replacement_axiom]
  --
  -- toSubtype это «зависимая пара»:
  -- значение несёт в себе объект вместе с доказательством его принадлежности множеству.
  --
  -- A.toSubtype = { x : Object // x ∈ A }, то есть тип пар ⟨x, hx⟩,
  -- где `x : Object` и `hx : x ∈ A`.
  constructor
  · rintro ⟨s, hx⟩
    rw [← mem_singleton] at hx
    exact hx
  · rintro hx
    have h3 : (3 : Object) ∈ ({3, 5, 9} : Set) := by
    -- mem_triple (x a b c : Object) : x ∈ ({a,b,c} : Set) ↔ (x = a ∨ x = b ∨ x = c)
      refine (mem_triple 3 3 5 9).mpr ?_
      left; rfl
    have hx1 : x = 1 := by
      rw [← mem_singleton]
      exact hx
    -- use ⟨3, h3⟩
    -- Не очевидно, что x_1 имеет тип {3, 5, 9} : Set
    show ∃ x_1 : ({3, 5, 9} : Set), x = 1
    -- тип x_1 нигде явно не печатается в цели — он берётся из области определения `P` в
    -- `replacement_axiom {P : A → Object → Prop}`, где `A` через CoeSort означает `A.toSubtype`.
    -- Ниже разъяснено, как поступать в подобных ситуациях.
    refine ⟨?_, hx1⟩
    exact ⟨3, h3⟩

-- Заметка на будущее: если не понятно, что подставить в `∃ x_1, ...` —
-- не гадай по напечатанной цели (тип связанной переменной там всегда скрыт).
-- Вместо этого:
-- 1) refine ⟨?_, ...⟩, чтобы Lean показал точный тип дыры;
-- 2) если тип — это A.toSubtype, вспомни, что это снова пара ⟨val, proof⟩;
-- 3) при необходимости смотри исходную сигнатуру леммы (replacement_axiom),
--    откуда взялся экзистенциал, а не уже инстанцированную цель.

/-- Exercise 3.1.5.  Здесь можно использовать тактики {tactic}`tfae_have` и
    {tactic}`tfae_finish`.
    TFAE: The Following (propositions) Are Equivalent — следующие утверждения равносильны. -/
theorem SetTheory.Set.subset_tfae (A B : Set) :
  [A ⊆ B, A ∪ B = B, A ∩ B = A].TFAE := by
    tfae_have (h1iff2 : A ⊆ B ↔ A ∪ B = B) : 1 ↔ 2 := by
      constructor
      · rw [union_comm]
        exact union_subset -- (hAX : A ⊆ X) : X ∪ A = X
      · intro h
        rw [← h]
        intro x hx
        rw [mem_union] -- x ∈ X ∪ Y ↔ x ∈ X ∨ x ∈ Y
        left
        exact hx
    tfae_have (h2iff3 : A ∪ B = B ↔ A ∩ B = A) : 2 ↔ 3 := by
      constructor
      · intro h
        ext x
        rw [mem_inter]
        tauto
      · intro h
        rw [← h1iff2]
        rw [subset_def]
        intro x hx
        rw [← h] at hx
        rw [mem_inter] at hx
        exact hx.2
    tfae_finish

-- Это доказательство отличается от того, что я сделал на бумаге тем,
-- Что здесь мы показываем равносильности вместо импликаций,
-- поэтому нам не требуется зацикливать рассуждение:
--
-- A ⊆ B ↔ A ∪ B = B ↔ A ∩ B = A
--       ^           ^
--    h1iff2      h2iff3

-- Немного более подробная версия без применения тактики tauto
theorem SetTheory.Set.subset_tfae' (A B : Set) :
  [A ⊆ B, A ∪ B = B, A ∩ B = A].TFAE := by
    tfae_have (h1iff2 : A ⊆ B ↔ A ∪ B = B) : 1 ↔ 2 := by
      constructor
      · rw [union_comm]
        exact union_subset -- (hAX : A ⊆ X) : X ∪ A = X
      · intro h
        rw [← h]
        intro x hx
        rw [mem_union] -- x ∈ X ∪ Y ↔ x ∈ X ∨ x ∈ Y
        left
        exact hx
    tfae_have (h2iff3 : A ∪ B = B ↔ A ∩ B = A) : 2 ↔ 3 := by
      constructor
      · intro h
        apply subset_antisymm -- (hAB : A ⊆ B) (hBA : B ⊆ A) : A = B
        · intro x
          rw [mem_inter] -- x ∈ X ∩ Y ↔ x ∈ X ∧ x ∈ Y
          tauto
        · rw [subset_def] -- X ⊆ Y ↔ ∀ x ∈ X, x ∈ Y
          intro x
          rw [mem_inter]
          intro hx
          refine ⟨hx, ?_⟩
          rw [← h1iff2] at h
          exact h x hx
      · intro h
        rw [← h1iff2]
        rw [subset_def]
        intro x hx
        rw [← h] at hx
        rw [mem_inter] at hx
        exact hx.2
    -- Это уже не требуется делать, потому что
    -- мы доказывали равносильности вместо импликаций.
    -- tfae_have (h3iff1 : A ∩ B = A ↔ A ⊆ B) : 3 ↔ 1 := by
    --   constructor
    --   · sorry
    --   · sorry
    tfae_finish

/-- Exercise 3.1.7 (a) -/
theorem SetTheory.Set.inter_subset_left (A B : Set) : A ∩ B ⊆ A := by
  rw [subset_def]
  intro x hx
  rw [mem_inter] at hx
  exact hx.1

/-- Exercise 3.1.7 (b) -/
theorem SetTheory.Set.inter_subset_right (A B : Set) : A ∩ B ⊆ B := by
  rw [inter_comm]
  exact inter_subset_left B A

/-- Exercise 3.1.7 (c) -/
@[simp]
theorem SetTheory.Set.subset_inter_iff (A B C : Set) : C ⊆ A ∩ B ↔ C ⊆ A ∧ C ⊆ B := by
  simp only [subset_def, mem_inter, forall_and]

#check forall_and -- (∀ (x : α), p x ∧ q x) ↔ (∀ (x : α), p x) ∧ ∀ (x : α), q x

/-- Exercise 3.1.7 -/
theorem SetTheory.Set.subset_inter_iff' (A B C : Set) : C ⊆ A ∩ B ↔ C ⊆ A ∧ C ⊆ B := by
  repeat rw [subset_def] -- simp only [subset_def]
  constructor
  · intro h
    constructor
    · intro x hxc
      specialize h x
      rw [mem_inter] at h
      exact (h hxc).1
    · intro x hxc
      specialize h x
      rw [mem_inter] at h
      exact (h hxc).2
  · intro h x hxc
    rw [mem_inter]
    -- Не буду тут повторять рассуждения, подобные тем, которые приведены выше.
    -- Следующая тактика вполне справится:
    tauto

/-- Exercise 3.1.7 (d) -/
theorem SetTheory.Set.subset_union_left (A B : Set) : A ⊆ A ∪ B := by
  intro x hx
  rw [mem_union] -- x ∈ X ∪ Y ↔ x ∈ X ∨ x ∈ Y
  left
  exact hx

/-- Exercise 3.1.7 (e) -/
theorem SetTheory.Set.subset_union_right (A B : Set) : B ⊆ A ∪ B := by
  rw [union_comm] -- A ∪ B = B ∪ A
  exact subset_union_left B A

/-- Exercise 3.1.7 (f) -/
@[simp]
theorem SetTheory.Set.union_subset_iff (A B C : Set) : A ∪ B ⊆ C ↔ A ⊆ C ∧ B ⊆ C := by
  simp only [subset_def, mem_union, or_imp, forall_and]

#check or_imp -- a ∨ b → c ↔ (a → c) ∧ (b → c)

/-- Exercise 3.1.7 -/
theorem SetTheory.Set.union_subset_iff' (A B C : Set) : A ∪ B ⊆ C ↔ A ⊆ C ∧ B ⊆ C := by
  simp only [subset_def, mem_union]
  constructor
  · intro h
    split_ands
    · tauto
    · intro x hxb
      specialize h x
      tauto
  · intro h x hxab
    tauto

#check subset_antisymm -- (A B : Set), A ⊆ B → B ⊆ A → A = B

/-- Exercise 3.1.8 (a) -/
@[simp]
theorem SetTheory.Set.inter_union_cancel (A B : Set) : A ∩ (A ∪ B) = A := by
  apply subset_antisymm
  · exact inter_subset_left A (A ∪ B) -- A ∩ B ⊆ A
  · rw [subset_inter_iff] -- C ⊆ A ∩ B ↔ C ⊆ A ∧ C ⊆ B
    have h₁ := subset_self A
    have h₂ := subset_union_left A B
    exact ⟨h₁, h₂⟩

-- Интересное наблюдение:
-- A ∩ (A ∪ B) = A  ~~  min(a, max(a, b)) = a
--                      max(a, b) = b => min(a, b) = a
--                      max(a, b) = a => min(a, a) = a
-- A ∪ (A ∩ B) = A  ~~  max(a, min(a, b)) = a

/-- Exercise 3.1.8 (b) -/
@[simp]
theorem SetTheory.Set.union_inter_cancel (A B : Set) : A ∪ (A ∩ B) = A := by
  ext x
  simp only [mem_union, mem_inter]
  show (x ∈ A) ∨ (x ∈ A ∧ x ∈ B) ↔ x ∈ A
  tauto

/-- Exercise 3.1.9 (a) -/
theorem SetTheory.Set.partition_left {A B X : Set}
  (h_union : A ∪ B = X) (h_inter : A ∩ B = ∅) : A = X \ B := by
    -- Лучше бы было начать вот так:
    -- ext x
    -- Но увы, я был упорот или начал вот так:
    apply subset_antisymm
    · rw [subset_def]
      intro x hxa
      rw [mem_sdiff]
      constructor
      · rw [← h_union]
        rw [mem_union]
        left
        exact hxa
      · intro hxb
        have hxab : x ∈ A ∧ x ∈ B := ⟨hxa, hxb⟩
        rw [← mem_inter, h_inter] at hxab
        have hmem := not_mem_empty x
        contradiction
    · rw [subset_def]
      intro x
      rw [mem_sdiff]
      intro ⟨hx, hnb⟩
      rw [← h_union] at hx
      rw [mem_union] at hx
      rcases hx with ha | hb
      · exact ha
      · contradiction

-- Доказательство сгенерировано ИИ: введение промежуточной гипотезы `hnotab` мне было неочевидно.
theorem SetTheory.Set.partition_left' {A B X : Set}
  (h_union : A ∪ B = X) (h_inter : A ∩ B = ∅) : A = X \ B := by
    ext x
    rw [mem_sdiff, ← h_union, mem_union]
    -- Цель `x ∈ A ↔ (x ∈ A ∨ x ∈ B) ∧ x ∉ B` не тавтология сама по себе:
    -- нужен ещё факт, что x не может лежать в A и B одновременно —
    -- это и даёт неиспользованная h_inter.
    have hnotab : ¬ (x ∈ A ∧ x ∈ B) := by
      rw [← mem_inter, h_inter]
      exact not_mem_empty x
    tauto

/-- Exercise 3.1.9 (b) -/
theorem SetTheory.Set.partition_right {A B X : Set}
  (h_union : A ∪ B = X) (h_inter : A ∩ B = ∅) : B = X \ A := by
    ext x
    rw [mem_sdiff, ← h_union, mem_union]
    constructor
    · intro hxb
      constructor
      · right; exact hxb
      · intro hxa
        have h : x ∈ A ∧ x ∈ B := ⟨hxa, hxb⟩
        rw [← mem_inter, h_inter] at h
        have h' := not_mem_empty x
        contradiction
    · intro ⟨h, hxna⟩
      rcases h with hxa | hxb
      · contradiction
      · exact hxb

-- Пример использования тактики fin_cases.
-- Понимание работы этой тактики пригождается в доказательсве ниже.
example (n : ℕ) (h : n ∈ ({0, 1, 4} : Finset ℕ)) :
    n = 0 ∨ ∃ k, n = k ^ 2 ∧ k > 0 := by
  fin_cases h
  · left; rfl
  · right; exact ⟨1, rfl, by norm_num⟩
  · right; exact ⟨2, rfl, by norm_num⟩

/--
  Exercise 3.1.10.
  Возможно, пригодятся {name}`Function.onFun_apply` и тактика {tactic}`fin_cases`.
-/
-- Disjoint X Y означает X ∩ Y = ∅.
-- Доказать, что три множества A \ B, A ∩ B, B \ A попарно не пересекаются.
--
-- Pairwise r означает ∀ ⦃i j⦄, i ≠ j → r i j —
-- свойство r выполняется для любой пары различных элементов.
-- Работать с ним удобно через `intro i j hij`,
-- что даёт индексы i j и гипотезу hij : i ≠ j, оставляя доказать r i j.
--
-- Function.onFun f g — это функция, оборачивающая бинарное f,
-- подставляя в оба аргумента результат g:
--
-- onFun f g a b = f (g a) (g b) (лемма Function.onFun_apply).
--
-- Здесь g — массив ![A \ B, A ∩ B, B \ A] : Fin 3 → Set,
-- так что goal Pairwise (onFun Disjoint g) после intro
-- превращается в Disjoint (g i) (g j).
-- Т.е. мы берём попарно утверждения с соответствующими индексами.
--
-- `![a, b, c]` — векторная нотация Mathlib (`Matrix.vecCons`):
-- это функция `Fin 3 → α` с `0 ↦ a`, `1 ↦ b`, `2 ↦ c`,
-- поэтому её можно применять вот так: g i.
theorem SetTheory.Set.pairwise_disjoint (A B : Set) :
  Pairwise (Function.onFun Disjoint ![A \ B, A ∩ B, B \ A]) := by
    intro i j hij
    -- Тип i, j изначально выводится как Fin (Nat.succ 0).succ.succ
    -- (из-за вложенных Matrix.vecCons в ![A \ B, A ∩ B, B \ A]),
    -- что по определению есть числовой литерал Fin 3, но печатается по-другому.
    -- change просто перепечатывает тип, не меняя саму гипотезу.
    change Fin 3 at i j
    -- Разворачивает определение onFun в цели: goal
    -- Function.onFun Disjoint g i j превращается в Disjoint (g i) (g j),
    -- где g = ![A \ B, A ∩ B, B \ A].
    unfold Function.onFun
    rw [disjoint_iff]
    -- fin_cases i перебирает все конкретные значения i : Fin 3 (0, 1, 2),
    -- порождая отдельную цель на каждое
    -- Вместе с fin_cases j получаем по одной цели на каждую из 9 комбинаций (i, j).
    -- Соответственно мы получаем 9 утверждений о попарно непересекающихся множествах.
    fin_cases i <;> fin_cases j
      <;> simp -- Применяем функции к аргументам, редуцируя выражения.
      <;> (try contradiction) -- Устраняем несколько противоречий сразу же.
      <;> (ext x; simp [mem_inter, mem_sdiff]; tauto) -- Остальное тривиально-тавтологично.

-- Небольшое пояснение последних строчек в этом доказательстве:
--
-- После упрощения в контексте мы видим гипотезу
-- hij : (fun i ↦ i) ⟨0, ⋯⟩ ≠ (fun i ↦ i) ⟨0, ⋯⟩
-- Здесь с двух сторон от неравенства стоит один и тот же терм.
-- Это так, не смотря на то, что мы не видим второй элемент кортежа (который является доказательством).
-- А любые два доказательства типа Prop считаются равными друг другу по определению.
--
-- Proof irrelevance (нерелевантность доказательств) — фундаментальное правило равенства,
-- встроенное в ядро Lean 4: любые два терма одного и того же Prop
-- считаются определённо (definitionally) равными друг другу.
--
-- Ядро Lean, сравнивая два терма на равенство,
-- просто не смотрит на "внутреннее устройство" доказательства Prop —
-- оно автоматически признаёт любые два доказательства одного и того же утверждения идентичными.

#check compl_inter -- X \ (A ∩ B) = X \ A ∪ X \ B
#check compl_union -- X \ (A ∪ B) = X \ A ∩ (X \ B)
#check inter_compl -- A ∩ (X \ A) = ∅
#check inter_union_cancel -- A ∩ (A ∪ B) = A
#check union_inter_cancel -- (A ∪ A) ∩ B = A

-- `A ∪ B` разбивается на три попарно непересекающихся куска: то, что только в `A`,
-- то, что в обоих, и то, что только в `B`
/-- Exercise 3.1.10 -/
theorem SetTheory.Set.union_eq_partition (A B : Set) :
  A ∪ B = (A \ B) ∪ (A ∩ B) ∪ (B \ A) := by
    ext x
    simp only [mem_union, mem_sdiff, mem_inter]
    -- Здесь без классической логики не обойтись,
    -- и тактика tauto как раз неявно использует
    -- Classical.em (x ∈ B) и Classical.em (x ∈ A)
    -- Ниже мы докажем один раз полностью "вручную".
    tauto

-- тот же результат, что и `union_eq_partition`, но доказан вручную через `by_cases`
-- вместо `tauto`, чтобы явно увидеть, где используется исключённое третье
theorem SetTheory.Set.union_eq_partition' (A B : Set) :
  A ∪ B = (A \ B) ∪ (A ∩ B) ∪ (B \ A) := by
    ext x
    simp only [mem_union, mem_sdiff, mem_inter]
    constructor
    · intro h
      rcases h with hxa | hxb
      · -- x ∈ B неразрешимо конструктивно (нет Decidable-инстанса),
        -- поэтому явно применяем исключённое третье через by_cases
        by_cases hb : x ∈ B
        · left
          right
          exact ⟨hxa, hb⟩
        · left
          left
          exact ⟨hxa, hb⟩
      · by_cases ha : x ∈ A
        · left
          right
          exact ⟨ha, hxb⟩
        · right
          exact ⟨hxb, ha⟩
    · intro h
      guard_hyp h : ((x ∈ A ∧ x ∉ B) ∨ (x ∈ A ∧ x ∈ B)) ∨ (x ∈ B ∧ x ∉ A)
      -- Правый конъюнкт нас никогда не интересует,
      -- поэтому с помощью прочерка игнорируем его.
      rcases h with (⟨ha, -⟩ | ⟨ha, -⟩) | ⟨hb, -⟩
      · left
        exact ha
      · left
        exact ha
      · right
        exact hb

/--
  Exercise 3.1.11.
  Задача — доказать это, не используя
  {name}`Set.specify`, {name}`Set.specification_axiom`, {name}`Set.specification_axiom'` и
  ничего построенного из них (например, разности и пересечения).
-/
-- У нас есть произвольное множество A и предикат P на его элементах.
-- Нужно построить множество B ⊆ A такое, что x.val ∈ B ↔ P x для всех x ∈ A —
-- то есть B = {x ∈ A | P x}, обычная теоретико-множественная "вырезка".
-- Отличие от Set.specify в том, что здесь нельзя
-- пользоваться аксиомой спецификации (и всем, что из неё построено) —
-- множество B нужно получить через аксиому замены (replacement_axiom).
-- Ну, просто потому, что мы и строим сейчас аксиому спецификации из аксиомы замены (подстановки).
theorem SetTheory.Set.specification_from_replacement {A : Set} {P : A → Prop} :
  ∃ B, B ⊆ A ∧ ∀ x, x.val ∈ B ↔ P x := by
    -- Двухместное отношение для аксиомы замены (подстановки):
    -- y соответствует x, если P x выполнено и (y = x.val)
    let P' : A → Object → Prop := fun x y => P x ∧ (y = x.val)
    -- Однозначность (hP, требуется для replace): P' — функциональное отношение,
    -- т.к. y и y' оба вынуждены равняться x.val, а значит равны друг другу.
    have hP : ∀ (x : A) (y y' : Object), P' x y ∧ P' x y' → y = y' := by
      intro x y y'
      rintro ⟨⟨_hpx, hy⟩, _hpx', hy'⟩
      rw [hy, hy']
    -- B — образ A относительно P' (аксиома замены).
    set B := A.replace hP with hB
    -- ^^ `set` (в отличие от `let`) вводит B как непрозрачную константу
    -- и отдельную гипотезу hB : B = A.replace hP, так что определение B
    -- не сворачивается в "⋯" в списке гипотез, а видно явно.
    use B
    show B ⊆ A ∧ (∀ (x : A.toSubtype), ↑x ∈ B ↔ P x)
    constructor
    · -- B ⊆ A : всякий элемент B — это x.val для какого-то x ∈ A,
      -- а значит лежит в A
      intro y hy
      rw [hB, replacement_axiom] at hy
      obtain ⟨x, _hpx, hxy⟩ := hy
      rw [hxy]
      exact x.property
    · intro x
      rw [hB, replacement_axiom]
      constructor
      · -- Если x.val = x'.val для некоторого x' с P x',
        -- то x = x' (подтипы равны по равенству val), значит P x.
        rintro ⟨x', hPx', hxx'⟩
        have hxx : x = x' := Subtype.ext hxx'
        rw [hxx]
        exact hPx'
      · -- Если P x, то сам x годится в качестве свидетеля.
        intro hPx
        exact ⟨x, hPx, rfl⟩

-- Тот же результат, но линейно: без промежуточных `let`/`have` для предиката P' и hP —
-- предикат передаём прямо в `A.replace`, а его однозначность доказываем
-- как первую цель в общем списке.
theorem SetTheory.Set.specification_from_replacement' {A : Set} {P : A → Prop} :
  ∃ B, B ⊆ A ∧ ∀ x, x.val ∈ B ↔ P x := by
  refine ⟨A.replace (P := fun x y => P x ∧ y = x.val) ?_, ?_, ?_⟩
  · -- Однозначность : y и y' оба равны x.val, значит равны друг другу.
    rintro x y y' ⟨⟨_, hy⟩, _, hy'⟩
    rw [hy, hy']
  · -- B ⊆ A.
    intro y hy
    rw [replacement_axiom] at hy
    obtain ⟨x, _, hxy⟩ := hy
    rw [hxy]
    exact x.property
  · -- x.val ∈ B ↔ P x.
    intro x
    rw [replacement_axiom]
    constructor
    · rintro ⟨x', hPx', hxx'⟩
      rw [Subtype.ext hxx']
      exact hPx'
    · intro hPx
      exact ⟨x, hPx, rfl⟩


#check union_subset_iff   -- A ∪ B ⊆ C ↔ A ⊆ C ∧ B ⊆ C
#check subset_union       -- (hAX : A ⊆ X) : A ∪ X = X
#check subset_union_left  -- A ⊆ A ∪ B
#check subset_union_right -- B ⊆ A ∪ B
#check subset_trans       -- (hAB : A ⊆ B) (hBC : B ⊆ C) : A ⊆ C

/-- Exercise 3.1.12 (unions) -/
theorem SetTheory.Set.subset_union_subset {A B A' B' : Set}
  (hA'A : A' ⊆ A) (hB'B : B' ⊆ B) : A' ∪ B' ⊆ A ∪ B := by
    -- элементарное доказательство: раскрываем `⊆` до `∈` и явно разбираем случаи `x ∈ A'` и `x ∈ B'`
    rw [subset_def] at *
    intro x hx
    specialize hA'A x
    specialize hB'B x
    rw [mem_union] at *
    rcases hx with hxa | hxb
    · left
      exact hA'A hxa
    · right
      exact hB'B hxb

-- то же утверждение, но доказано "сверху": сводим цель к паре включений через `union_subset_iff`
-- и транзитивность `⊆`, вместо разбора случаев по элементам
theorem SetTheory.Set.subset_union_subset' {A B A' B' : Set}
  (hA'A : A' ⊆ A) (hB'B : B' ⊆ B) : A' ∪ B' ⊆ A ∪ B := by
    rw [union_subset_iff] -- A ∪ B ⊆ C ↔ A ⊆ C ∧ B ⊆ C
    constructor
    · have hA : A ⊆ A ∪ B := subset_union_left A B
      exact subset_trans hA'A hA
    · have hB : B ⊆ A ∪ B := subset_union_right A B
      exact subset_trans hB'B hB

/-- Exercise 3.1.12 (intersections) -/
theorem SetTheory.Set.subset_inter_subset {A B A' B' : Set}
  (hA'A : A' ⊆ A) (hB'B : B' ⊆ B) : A' ∩ B' ⊆ A ∩ B := by
    -- тот же приём, что и в `subset_union_subset'`: `subset_inter_iff` сводит цель
    -- к паре включений `A' ∩ B' ⊆ A` и `A' ∩ B' ⊆ B`
    rw [subset_inter_iff]
    constructor
    · have hA := inter_subset_left A' B'
      exact subset_trans hA hA'A
    · have hB := inter_subset_right A' B'
      exact subset_trans hB hB'B

/-- Exercise 3.1.12 (differences: a counterexample) -/
theorem SetTheory.Set.subset_diff_subset_counter :
  ∃ (A B A' B' : Set), (A' ⊆ A) ∧ (B' ⊆ B) ∧ ¬((A' \ B') ⊆ (A \ B)) := by
    -- Контрпример:
    -- A = A' = B = {x}, B' = ∅,
    -- где x — любой объект (берём x = ∅).
    --
    -- Тогда:
    -- A' \ B' = {x} \ ∅ = {x},
    -- но
    -- A \ B = {x} \ {x} = ∅
    set e : Object := ((∅ : Set) : Object)
    use {e}, {e}, {e}, ∅
    -- Цель:
    -- ({∅} ⊆ {∅} ∧ ∅ ⊆ {∅}) ∧ ¬({∅} \ ∅ ⊆ {∅} \ {∅})
    --
    -- simp only [subset_self, empty_subset, true_and]
    refine ⟨subset_self {e}, empty_subset {e}, ?_⟩
    intro h
    rw [subset_def] at h
    have hself : e ∈ ({e} : Set) := by rw [mem_singleton]
    have hmem : e ∈ ({e} : Set) \ ∅ := by
      rw [mem_sdiff]
      exact ⟨hself, not_mem_empty _⟩
    have hcontra := h e hmem
    rw [mem_sdiff] at hcontra
    obtain ⟨h₀, h1⟩ := hcontra
    contradiction

/-
  Финальная часть Exercise 3.1.12:
  Сформулируйте и докажите разумный положительный результат,
  заменяющий приведённую выше теорему, который включает разности множеств.
-/

/-- Exercise 3.1.13 -/
theorem SetTheory.Set.singleton_iff (A : Set) (hA : A ≠ ∅) :
  (¬∃ B ⊂ A, B ≠ ∅) ↔ ∃ x, A = {x} := by

    sorry

/-
  Теперь мы вводим связь между этим понятием множества и понятием множества из Mathlib.
  Упражнение ниже познакомит вас с API множеств Mathlib.
-/

instance SetTheory.Set.inst_coe_set : Coe Set (_root_.Set Object) where
  coe X := { x | x ∈ X }

-- Теперь можно превратить наш `Set` в Mathlib-евский `_root_.Set`.
-- Заметьте, что множества Mathlib параметризованы типом элементов, в нашем случае — `Object`.
example (X : Set) : _root_.Set Object := X

/--
  Инъективность приведения типа. Заметьте, однако, что мы НЕ утверждаем, что это приведение
  сюръективно (и парадокс Рассела действительно этому препятствует)
-/
@[simp]
theorem SetTheory.Set.coe_inj' (X Y : Set) :
    (X : _root_.Set Object) = (Y : _root_.Set Object) ↔ X = Y := by
  constructor
  . intro h; apply ext; intro x
    replace h := congr(x ∈ $h); simpa using h
  rintro rfl; rfl

/-- Совместимость операции принадлежности {kw (of := «term_∈_»)}`∈` -/
theorem SetTheory.Set.mem_coe (X : Set) (x : Object) : x ∈ (X : _root_.Set Object) ↔ x ∈ X := by
  simp

/-- Совместимость пустого множества -/
theorem SetTheory.Set.coe_empty : ((∅ : Set) : _root_.Set Object) = ∅ := by
  simp

/-- Совместимость подмножества -/
theorem SetTheory.Set.coe_subset (X Y : Set) :
  (X : _root_.Set Object) ⊆ (Y : _root_.Set Object) ↔ X ⊆ Y := by
    simp only [Set.setOf_subset_setOf]
    rfl

/-- Совместимость строгого подмножества -/
theorem SetTheory.Set.coe_ssubset (X Y : Set) :
    (X : _root_.Set Object) ⊂ (Y : _root_.Set Object) ↔ X ⊂ Y := by sorry

/-- Совместимость одноэлементного множества -/
theorem SetTheory.Set.coe_singleton (x : Object) : (({x} : Set) : _root_.Set Object) = {x} := by sorry

/-- Совместимость объединения -/
theorem SetTheory.Set.coe_union (X Y : Set) :
    ((X ∪ Y : Set) : _root_.Set Object) = (X : _root_.Set Object) ∪ (Y : _root_.Set Object) := by sorry

/-- Совместимость пары -/
theorem SetTheory.Set.coe_pair (x y : Object) : (({x, y} : Set) : _root_.Set Object) = {x, y} := by sorry

/-- Совместимость подтипа -/
theorem SetTheory.Set.coe_subtype (X : Set) :  (X : _root_.Set Object) = X.toSubtype := by sorry

/-- Совместимость пересечения -/
theorem SetTheory.Set.coe_intersection (X Y : Set) :
    ((X ∩ Y : Set) : _root_.Set Object) = (X : _root_.Set Object) ∩ (Y : _root_.Set Object) := by sorry

/-- Совместимость разности множеств -/
theorem SetTheory.Set.coe_diff (X Y : Set) :
    ((X \ Y : Set) : _root_.Set Object) = (X : _root_.Set Object) \ (Y : _root_.Set Object) := by sorry

/-- Совместимость непересекаемости -/
theorem SetTheory.Set.coe_Disjoint (X Y : Set) :
    Disjoint (X : _root_.Set Object) (Y : _root_.Set Object) ↔ Disjoint X Y := by sorry

end Chapter3
